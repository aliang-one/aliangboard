# 手机适配 Wave 5(W3+W4+W5)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清掉审计余下 171 条发现(W3 详情页全族 73 / W4 列表+向导+workbench+admin+设置 98),并以 mobile-guard 静态守卫 + 全量 390px 截图收口(原 W5 范围)。

**Architecture:** 守卫先行(T0 建立 M1/M2 门禁 + 违规 allowlist),此后每个批次修复自己文件并从 allowlist 删除条目,allowlist 单调收缩、W5 末清零;配方全部复用 W2 已在真代码上定型的实现(范例文件锚点见 §配方),零新词汇。

**Tech Stack:** Vue 3 + Tailwind;vitest/happy-dom;node --test 静态守卫;Playwright 实机验收。零新增依赖。

**Spec:** `docs/superpowers/specs/2026-09-09-mobile-adaptation-wave5-design.md`(§4 配方 R1-R12 权威定义;发现注册表 `2026-09-09-mobile-audit-findings.json`)

## Global Constraints

- 提交作者恒为 `aliang-one <aliangdone@gmail.com>`(repo config 已配);提交信息英文;禁止 Co-Authored-By;禁止改写已推送历史;工作分支 `feat/mobile-wave5-w345`,末尾一次 `--no-ff` 合 main(Ruling:spec「每波独立分支」原为多会话并行设计,本三波同一会话顺序执行,波界在分支上跑全量门禁,合并后 main 复跑一次)。
- 零新增依赖;`max-sm:` 类优先,JS 分支(isPhone)仅用于结构切换。
- 四件套完整形态:`relative max-sm:after:absolute max-sm:after:-inset-2 max-sm:after:content-['']`;z 一律取 `Z` 阶梯;src 无中文字面量(i18n 门禁)。
- 桌面零回归是每条改动的硬约束(spec §3);所有配方均为增量断点类或 isPhone 分支。
- 每批次完成:`npm run typecheck` + 本批文件相关既有测试(`ls src/views/__tests__/<ViewName>*` / `src/components/**/__tests__/`)+ `node --test scripts/mobile-guard.test.mjs`;波界跑全量四道门禁。

## 配方(W2 落地范例锚点,照抄不改义)

- **R4 页头配方** → 范例 `src/views/NsWorkloadDetail.vue` 页头(搜 `flex flex-wrap items-start justify-between`):外层加 wrap/gap、左块 `min-w-0`(**中间普通 div 也要 min-w-0**)、h1 `min-w-0 max-sm:truncate` + `:title`、按钮组 `flex flex-wrap gap-xs`(**去 shrink-0**)、meta 行保持 wrap。
- **R3 tab 横滚** → 范例同文件 tab 条(搜 `overflow-x-auto border-b`):容器加 `overflow-x-auto`,按钮加 `shrink-0 whitespace-nowrap`。
- **R5 裸表迁 DataTable** → 范例 `src/views/NsWorkloadDetail.vue` Revisions/Events(搜 `revHeaders`)与 `src/views/WorkbenchServers.vue`:headers 数组(用 computed 保持响应式)+ row-key(无唯一键注入 `_idx`)+ 每列 slot 双分支;外层旧圆角卡壳删掉;桌面信息等价(消息类长文本列保留 `max-w-[Npx]` 上限+truncate+title);回滚类行钮四件套。**语义特殊表保底**:外层 `overflow-x-auto` + 表 `min-w-[600px]` + 单元格 truncate(见 NsNetworkPolicyDetail rules)。
- **R8 表单行** → 行容器 `flex flex-wrap`(或 `max-sm:flex-col`),`flex-1` 输入补 `min-w-0` 必要时 `max-sm:w-full`,原生 select 补 `w-full`/`max-sm:min-h-[40px]`。
- **R9 触控** → 文字钮/pills:`max-sm:min-h-[40px]`;图标钮:四件套;select:`max-sm:min-h-[40px]`(select 挂不了伪元素)。
- **R10 长内容/网格** → chips 值 `truncate`/`break-all`+外层 `min-w-0`;`grid-cols-2 → grid-cols-1 sm:grid-cols-2`、`grid-cols-3/4 → grid-cols-2 sm:grid-cols-3/4`(Modal 内 3 列可 `grid-cols-1 sm:grid-cols-3`);固定双栏(如 `grid-cols-[220px_1fr]`)手机改单列上下。
- **R6-lite 双栏折叠** → 范例 `src/views/WorkbenchDetail.vue`(搜 `:disabled="!isPhone"`):Teleport :disabled 单 DOM;轻量场景(文件列表栏)可用 `max-sm:hidden` 列 + 顶部下拉切换,或上下堆叠(本波取**上下堆叠**:左栏 `max-sm:max-h-[30vh] overflow-y-auto` + 右栏自适应,零 JS,选简单方案)。
- **R7 SSH 终端配件** → 从 `src/components/common/InteractiveTerminal.vue` 抽 `src/components/common/TerminalKeyBar.vue`(props: send;内部 KEY_BYTES)+ `src/composables/useTerminalFont.js`(clamp 8-20 + fit 重排);两个终端组件共同消费;`InteractiveTerminal.keys.test.js` 迁移为双消费方断言。
- **纯 bug**:PVDetail t() 未插值(7 处)包 `{{ }}`;Settings 保存钮 `bg-primary text-on-primary` 文字色改对;`bg-primary/8` → `/10`(NsIngressDetail)。

## 任务与批次

每批次一个派发、一个 commit。批次表(附录)是需求的逐条清单;实现者对每行应用其配方提示列,行内信息不足以定位时读该文件核实。

---

### T0: mobile-guard 静态守卫先行(W5 提前,Ruling 见 Global Constraints)

**Files:** Create `scripts/mobile-guard.test.mjs`;Modify `package.json`(test:server 链追加,对齐 overflow-guard 的挂载方式)

- [ ] Step 1: 写守卫(先例 `scripts/overflow-guard.test.mjs` 的扫描/allowlist 结构):
  - **M1 裸表须可滚**:扫 src/**/*.vue(排除 `DataTable.vue` 自身);文件含 `<table` 且不含 `overflow-x-auto` 且未 import `DataTable` → 违规。
  - **M2 固定宽阈值**:任意值类 `w-[Npx]`/`max-w-[Npx]` N≥640 且同 class 串不含 `min(` → 违规。
  - 两规则各带 allowlist 数组;**初始种子 = 扫描全仓当前违规名单**(实现者跑出名单填入,逐条带 file 注释)。
- [ ] Step 2: `node --test scripts/mobile-guard.test.mjs` 绿(allowlist 生效);`npm test` 绿(守卫入链)。
- [ ] Step 3: Commit `feat(mobile): add mobile-guard M1/M2 static rules with violator allowlist (Wave5 T0)`

### W3 批次(B1-B5,每批步骤同构)

每批:
- [ ] Step 1: 读附录本批表逐行应用配方;同文件其余显性同类问题顺手修(审计只记了 ≥P2 阈值以上的)
- [ ] Step 2: `npx vitest run` 本批相关既有测试文件 + `npm run typecheck` + `node --test scripts/mobile-guard.test.mjs`(本批文件须已出 allowlist;M1/M2 违规清零的批次同步删 allowlist 条目)
- [ ] Step 3: Commit `fix(mobile): <批次名> recipes (Wave5 <Bn>)`

结构性特记:
- **B1 DataKeysEditor** `grid-cols-[220px_1fr]` → `grid-cols-1 sm:grid-cols-[220px_1fr]`;**NsConfigMapDetail Data tab 双栏** → R6-lite 上下堆叠(左文件列表 `max-sm:max-h-[30vh]` + 滚动)。
- **B2 NsRoleDetail rules/bindings、NsRoleBindingDetail subjects、NsServiceAccountDetail secrets** → 迁 DataTable;rules 表(chips 矩阵)如迁移代价过高走保底 `overflow-x-auto`+`min-w-[600px]`(实现者判断,报告里记选择)。
- **B3 NsNetworkPolicyDetail rules 表走保底**;NsIngressDetail 顺手 `bg-primary/8 → /10` 全清(为 T0/V5 收口)。
- **B4 NsHPADetail metrics 表迁 DataTable**。
- **B5 CrdDetail instances 表(P0)迁 DataTable**;PodDetail Files SplitPane 手机默认竖切(查该组件 orientation prop 默认值改 isPhone 三元);PVDetail t() 纯 bug;WorkloadDetail legacy 管理的 Pod 表 → 保底横滚(legacy 页低投入);ResourceReferences 迁 DataTable。

- [ ] W3 波界:`npm test && npm run test:unit && npm run typecheck && npm run build` 全绿;mobile-guard allowlist 只减不增。

### W4 批次(B6-B11)

- **B6(结构性,独立任务)**:R7 抽 TerminalKeyBar + useTerminalFont;InteractiveTerminal 与 SshTerminal 双消费;keys 测试迁移为断言两个消费方(SshTerminal 模板含 `TerminalKeyBar` + `v-if="isPhone"`;字号钮在场);SshTerminal 文件加四件套无——其已有触控类核对即可。
- **B7**:TerminalTaskbar 菜单 left 钳制(`Math.min(menuLeft, innerWidth - 菜单宽 - 8)` 或 max-sm 右锚 `right-4 left-auto`,对齐同文件 overflow 面板先例)+ 行钮四件套 + chip 本体/closeAll/「+」`max-sm:min-h-[40px]`(复核 iconMode 显影);WorkbenchShell tab 条 R3;Ledger 控件组拆 wrap(`max-sm:flex-wrap` + select `max-sm:w-full`)+ 蒸馏弹窗 `grid-cols-2 → grid-cols-1 sm:grid-cols-2`;Records 行(状态 chip truncate + 工具名 `max-sm:hidden` 或 truncate + 路径 break-all);WorkbenchChat 发送/停止钮 `max-sm:min-h-[40px] max-sm:min-w-[40px]`;Projects 换绑 select `max-sm:min-h-[40px]`。
- **B8(可拆两次派发)**:各列表页——页头 R4 化(标题+钮组 wrap/去 shrink-0)、页级 tab 条 R3、actions 槽图标钮四件套、创建/刷新钮 `max-sm:min-h-[40px]`、过滤 pills 同款;NsWorkloads `grid-cols-4 → grid-cols-2 sm:grid-cols-4` + image slot truncate;PriorityClasses **补 @row-click 跳详情**(卡片点卡无反应是功能缺口);NsRBAC/NsServices name 槽补 truncate(自定义槽丢了默认 truncate);ClusterCerts 过滤 pill;CrdList name 链接 truncate;NamespaceOverview 页头 R4 + 两小跳转钮四件套。
- **B9**:DeployApp KV/端口/nodeSelector 行 R8 + 六步指示器 `max-sm:overflow-x-auto`(行 `shrink-0`)+ 底操作行钮 `max-sm:min-h-[44px]`;NsIngress 注解行、Storage SC parameters 行、GroupsGrants 新建组行 R8。
- **B10**:ApiKeyManagement boundSA 单元格(卡片值区改 `flex flex-wrap` + mono 名 `break-all` + SSH 开关独立行)+ 三处 `grid-cols-2 → grid-cols-1 sm:grid-cols-2`;LlmConfig 状态区 `grid-cols-3 → grid-cols-1 sm:grid-cols-3` + 操作行 wrap + `max-sm:min-h-[40px]`;AiBehaviorConfig 工具行(名 `truncate min-w-0` + 行 wrap);AuditTrail 页头 R4;GroupsGrants 组行(`max-sm:hidden` 计数或双行)+ 两添加钮四件套;ToolOverrideEditor chip `max-sm:min-h-[40px]`;NsAllowlistEditor × 钮改块级 `px-1.5 py-0.5`+四件套;DeployIngressControllerDialog `grid-cols-2 → grid-cols-1 sm:grid-cols-2` + 返回钮 `max-sm:min-h-[40px]`。
- **B11**:Settings components 表迁 DataTable、保存钮 `text-on-primary`(纯 bug)、w-56 标签行 `max-sm:flex-col`(标签 `max-sm:w-full`)、MCP/MFA 开关容器四件套同级命中区核对;UserProfile tabs R3;SecuritySection/TokensSection 图标钮四件套;LogPopup 头(定位串 `truncate min-w-0`+关窗钮 `max-sm:min-h-[40px]`);LocaleToggle 四件套;ClusterCard 钮四件套/min-h;Clusters/Namespaces 页头钮 min-h+四件套;ClusterEvents/ClusterOverview/AuditLogs/NsEvents/MonitoringCenter pills/钮四件套+网格断点;RbacCanI 行 break-all;PortForwardPanel 行 truncate+钮四件套;DataTable 手机展开钮 `p-xs → relative max-sm:after:absolute max-sm:after:-inset-2 max-sm:after:content-['']` + `max-sm:min-h-[40px] min-w-[40px]`;Nodes cordon/drain 图标钮四件套+`:title` 保留。
- [ ] W4 波界:四道门禁全绿。

### W5 收口(B12)

- [ ] Step 1: allowlist 清零核验——M1/M2 allowlist 数组为空(或仅剩带裁决注释的豁免);V5 幽灵类全仓 grep `/8` 归零 → **删除 overflow-guard V5_ALLOWLIST 机制**。
- [ ] Step 2: 全量 390px 截图 sweep(orchestrator,Playwright,admin2 会话):全部 W3/W4 触达路由 + 列表页巡检,`docs/mobile-shots/2026-09-09-wave5-w345/`;每页 `document.documentElement.scrollWidth <= 390` 断言 + 截图。
- [ ] Step 3: 终审(opus)整分支评审 + 单修复轮 + scoped 复审。
- [ ] Step 4: 四道门禁终跑 → `--no-ff` 合 main → 合并后 main 复跑门禁 → 汇报。

---

## 附录:批次发现表(171 条,逐行应用)

#### B1:CM/Secret 域详情(10 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/components/common/DataKeysEditor.vue:126` | P1 | A+L: 固定 grid-cols-[220px_1fr] 无手机变体,Modal 手机全屏(p-lg)内容宽 326px,右栏编辑区仅 ~90px,值 textarea/CodeViewer 挤成窄条,ConfigMa | max-sm:grid-cols-1(上下堆叠,键列表限高)或左栏 max-sm 收成横向滚动条 |
| `src/views/NsConfigMapDetail.vue:231` | P1 | H+I: h1 无 truncate/break-all 且中间 flex 子项无 min-w-0,K8s 名为不可断单 token,>10 字符即把右侧删除钮推出屏并触发整页横滚(326px 内 icon 56+gap | truncate V2:中间子项 min-w-0 + h1 truncate;或 max-sm 缩 displ |
| `src/views/NsConfigMapDetail.vue:246` | P1 | E: tab 条 flex 无 wrap 无 overflow-x-auto,按钮 px-xl(48px/个)+标签: EN 五 tab ≈520px、ZH ≈390px > 手机内容宽 326px,EN 下 Annot | overflow-x-auto(同 DataTable 桌面包裹层配方)或 max-sm 允许换行/缩 px |
| `src/views/NsConfigMapDetail.vue:258` | P1 | A+L: 左栏固定 w-56(224px) shrink-0 无手机收起/切换,390px 下右栏仅约 86-100px,文件名头(289 行 span 无 truncate)与 CodeViewer 在窄条里挤爆,编辑 | max-sm:flex-col 收起为上下两段(列表限高)+桌面保留双栏;文件名 span 补 truncat |
| `src/views/NsConfigMapDetail.vue:296` | P2 | G: ~28px 触控目标无 after 四件套(左栏 271 行的 close 钮已配四件套,此处漏) | max-sm:after 四件套 |
| `src/views/NsConfigMapDetail.vue:383` | P2 | C: 按钮横排+input min-content(≈170px)合计 ≈328px > 卡内可用 262px,卡 overflow-hidden 把取消钮裁掉;同文件注解编辑已用 flex-col(既有修复范式未复用) | 照抄本文件注解编辑的 flex flex-col gap-xs 配方 |
| `src/views/NsSecretDetail.vue:204` | P1 | H+I: h1 无 truncate 且徽章行无 wrap:kubernetes.io/service-account-token 等长 type 名不可断,与 h1 一起把删除钮推出屏、整页横滚 | min-w-0+truncate(V2)+徽章行 max-sm:flex-wrap |
| `src/views/NsSecretDetail.vue:221` | P1 | E: 同 NsConfigMapDetail:246,手机上尾部 tab(Labels/YAML)截断不可达,需整页横滚 | overflow-x-auto / tab 条 max-sm 换行 |
| `src/views/NsSecretDetail.vue:267` | P2 | G: 三个 ~28px 图标钮无命中区扩展,reveal(查密文明文)是 Secret 详情核心操作,手机连续误触 | max-sm:after 四件套 |
| `src/views/NsSecretDetail.vue:282` | P2 | C: shrink-0 按钮行不可缩+textarea min-content ≈328px > 262px,卡 overflow-hidden 裁掉取消钮(同文件注解编辑 324 行为 flex-col 正解) | flex flex-col gap-xs(照抄注解编辑配方) |

#### B2:RBAC 域详情(12 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/views/NsRoleBindingDetail.vue:99` | P1 | H+I: 同 SA 详情:101,长 RoleBinding 名把编辑/删除推出屏,整页横滚 | min-w-0+truncate(V2)或 max-sm 纵排 |
| `src/views/NsRoleBindingDetail.vue:131` | P2 | B+I: 同 NsRoleDetail:174,roleName(system:node-proxier 等)mono 不可断,155px 格装不下即溢出卡片 | max-sm:grid-cols-1 + break-all |
| `src/views/NsRoleBindingDetail.vue:209` | P1 | D: 三列+px-lg×3 min-content ≈450px > 326px,卡 overflow-hidden 硬裁,namespace 列(subjects tab 的主内容之一)手机不可达 | overflow-x-auto 包裹或改键值行列表 |
| `src/views/NsRoleBindingDetail.vue:271` | P2 | B: 同 NsRBAC:368,手机全屏弹窗下 kind select ~98px,ClusterRole 选项文本截断 | max-sm:grid-cols-1 |
| `src/views/NsRoleBindingDetail.vue:298` | P1 | C: 单行 flex 无 wrap 无 max-sm:flex-col,kind=ServiceAccount 时三输入+40px 删除钮 min-content ≈530px > 弹窗内容 ~262px,行溢出、删除  | max-sm:flex-col(每字段占满一行,删除钮对齐行尾) |
| `src/views/NsRoleDetail.vue:143` | P2 | H+I: 同 CM 详情:143,ClusterRole 长名(system:controller:...-controller)必溢出,删除钮被推出屏;同页 Breadcrumbs 尾项同名同溢出 | min-w-0 + truncate(V2 配方) |
| `src/views/NsRoleDetail.vue:161` | P1 | E: 四 tab px-xl+英文标签 ≈410px > 326px,yaml tab(唯一直编入口)被裁到边缘,需整页横滚才可点 | overflow-x-auto 或 tab 条 max-sm:overflow-x-auto+缩 px |
| `src/views/NsRoleDetail.vue:174` | P2 | B+I: grid-cols-2 无 sm:/max-sm: 变体,手机每格 ~155px,ClusterRole 长名/长 namespace mono 无 break-all 直接穿透卡片圆角溢出 | grid 断点前缀(max-sm:grid-cols-1)+值 span 补 break-all |
| `src/views/NsRoleDetail.vue:233` | P1 | D: 未走 DataTable 且无 overflow-x-auto,外层卡 overflow-hidden 把超出部分硬裁: 多 chip 规则行 min-content ≈460px > 326px,最右 actio | overflow-x-auto 包 table(DataTable 桌面配方)或迁 DataTable 卡片化 |
| `src/views/NsRoleDetail.vue:291` | P1 | D: 五列 min-content(name mono+subjects chips+px-lg×5)≈600px+,卡 overflow-hidden 硬裁,手机上 subjects/age 列信息不可达 | overflow-x-auto 包裹或 DataTable 卡片化 |
| `src/views/NsServiceAccountDetail.vue:101` | P1 | H+I: 双按钮(编辑+删除 ≈190px)+icon 80px 后 h1 可用宽仅 ~37px,任何正常 SA 名即溢出,编辑/删除被推出屏需整页横滚 | min-w-0+truncate(V2);或 max-sm 头部纵排(标题行+按钮行) |
| `src/views/NsServiceAccountDetail.vue:192` | P2 | D: type 徽章 kubernetes.io/service-account-token 等长串无换行,四列 min-content 远超 326px,卡 overflow-hidden 硬裁,keys/age 列手 | overflow-x-auto 包裹或 DataTable 卡片化 |

#### B3:网络域详情+编辑器(11 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/components/networkpolicy/NpPeerEditor.vue:36` | P1 | 两个 flex-1 输入(flex-[size=20] 内在宽 ≈202px each)无 min-w-0 不换行,且嵌在 NpRuleEditor 行内(pl-md 16 + 删除钮 26 + gap 8 后可用仅 ≈ | max-sm:flex-col 堆叠(两个输入 max-sm:w-full) |
| `src/components/networkpolicy/NpRuleEditor.vue:72` | P2 | 带 endPort 时行 min-content ≈376px > 可用 ≈292px(342−pl-md−删除钮)→ 行溢出弹窗、endPort 框被切;且行删除钮 p-xs ≈26px 无四件套 | max-sm:flex-col(端口三元组占整行,删除钮 self-end)+ 补 max-sm:after  |
| `src/components/networkpolicy/NpSelectorEditor.vue:51` | P1 | 创建向导两处行均无 wrap/min-w-0:label 行 min-content ≈463px、表达式行(含 DoesNotExist 下拉 ≈120px)≈574px,均远超手机 Modal 内宽 342px →  | max-sm:flex-col 逐字段堆叠(输入 max-sm:w-full);删除钮补 max-sm:aft |
| `src/views/NsIngressDetail.vue:348` | P2 | py-0.5(2px)→ ≈24px 高的导航入口且 font-mono text-xs 文字小,无四件套;本页其余小钮均已按四件套处理,漏此 chip 组 | 补 max-sm:after 四件套或 max-sm:min-h-[36px] |
| `src/views/NsNetworkPolicyDetail.vue:103` | P1 | 整行无 flex-wrap、左列无 min-w-0、h1 无 truncate:NetworkPolicy 名是长单词(如 deny-all-by-default ≈323px @display-lg),左侧 min-c | 页头 flex-wrap gap-sm + 左列 min-w-0 + h1 truncate + meta 行 |
| `src/views/NsNetworkPolicyDetail.vue:128` | P1 | 4 个 tab 仅 padding 就各占 64px,自然宽 ≈550px > 326px 且无 overflow-x-auto/flex-wrap → YAML tab(整页唯一全量编辑入口)手机截断不可直达;flex | tab 条加 overflow-x-auto(子项 whitespace-nowrap)或 flex-wrap |
| `src/views/NsNetworkPolicyDetail.vue:211` | P1 | 3 列每格 px-lg=64px,#/From/Ports min-content 合计常超 326px:describePeer 产出不可断长串(如 Namespace: kubernetes.io/metadata. | 表格外包 overflow-x-auto + describePeer 文本加 break-all(或手机改卡 |
| `src/views/NsNetworkPolicyDetail.vue:322` | P1 | 两个 flex-1 输入(px-md py-sm text-body-md font-mono,size=20 内在宽 ≈202px)无 min-w-0 不换行:行 min-content ≈473px > 手机全屏 M | 行加 max-sm:flex-col 堆叠(删除钮 max-sm:self-end)或输入加 min-w-0  |
| `src/views/NsServiceDetail.vue:466` | P2 | Edit 主操作 ≈24px 高、加端口/加后端 ≈22px 高,均无四件套/max-sm:min-h;本页唯一带四件套的是 L556 删端口钮,其余小钮在触屏上难命中 | 统一补 max-sm:min-h-[40px](或 max-sm:after 四件套) |
| `src/views/NsServiceDetail.vue:537` | P2 | 未走 DataTable 且外层卡片是 rounded-lg overflow-hidden 无 overflow-x-auto:NodePort 型 Service 或命名端口较长(如 https-backend/ho | 表格外包一层 overflow-x-auto(仓库裸表标准配方),或手机改键值列表 |
| `src/views/NsServiceDetail.vue:792` | P1 | 与创建弹窗同型:行 min-content ≈369px > 手机 Modal 内宽 342px,无 wrap/min-w-0 → 行删除钮在手机被切出弹窗可视区 | max-sm:flex-col 堆叠或 flex-wrap + 输入 min-w-0(同 NsServices |

#### B4:存储/配额/HPA 详情(11 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/views/NsHPADetail.vue:81` | P1 | 详情页头横排不可折叠:左侧 56px 图标 + h1(32px 粗体,HPA 名为不可断行 DNS token)+ 无 wrap 的 meta 行(StatusChip+HPA 徽标+『Target: Deploymen | 表头容器加 max-sm:flex-col max-sm:items-start,meta 行加 flex-w |
| `src/views/NsHPADetail.vue:121` | P2 | 两列网格无断点前缀(手机仍 2 列),每卡内容宽仅 ~123px,而 p 内是 mono 不可断 token『Deployment/api-gateway』(~158px)无 truncate/break-all,常见目 | grid 断点前缀 max-sm:grid-cols-1;mono 值加 truncate 或 break-a |
| `src/views/NsHPADetail.vue:251` | P1 | 裸 <table> 6 列(metricName/type/targetType/targetValue/currentValue/status)未走 DataTable 卡片化,外层卡片是 overflow-hidde | 把 table 包进 overflow-x-auto 容器(照 DataTable 桌面分支配方),或手机档改 |
| `src/views/NsHPADetail.vue:308` | P2 | 两列网格无断点前缀,每卡内容宽 ~107px,内部 flex justify-between 标签/值行(Average Utilization + 80% 等)挤压换行 2-3 行,明显拥挤错位 | grid 断点前缀 max-sm:grid-cols-1(仓库既有 grid-cols-1 md:grid-c |
| `src/views/NsLimitRangeDetail.vue:139` | P1 | 同前两详情页头行破绽:无 wrap/min-w-0/truncate,56px 图标 + gap-lg + 32px 名 + 双钮 ~180px,名超 ~9 字符即总宽超 326px,按钮被推出视口、横向溢出 | flex-wrap gap-sm + 名称块 min-w-0 truncate(NsServiceDetail |
| `src/views/NsPDBDetail.vue:88` | P1 | 同款头部破绽且更重:『PodDisruptionBudget』徽标是不可断 token(~160px)+Age+h1 32px 粗体名+Edit/Delete 带字按钮,最小内容 ~448px vs 326px,操作按钮 | 同 NsHPADetail:max-sm:flex-col max-sm:items-start + 徽标行  |
| `src/views/NsPVCDetail.vue:116` | P1 | 头行无 flex-wrap、左块与 h1 无 min-w-0/truncate:56px 图标 + gap-lg(24) + 32px 加粗不可断行名 + 编辑/删除双钮(约196px),短名(8字符)也达 ~375px | 套用 NsServiceDetail.vue:443 同型配方:头行加 flex-wrap gap-sm,名称 |
| `src/views/NsPVCDetail.vue:235` | P2 | 图标钮仅 icon 24px + p-xs(4px)=32px 见方且为唯一入口(无文字冗余),低于 40px 触控标准,无 max-sm:after 命中区扩展;无 hover 时 title 提示在触屏也不显示 | max-sm:after 四件套(按钮自身 relative? 已是普通流——补 relative + max |
| `src/views/NsPVCDetail.vue:251` | P2 | 文件名 mono 无 truncate/min-w-0(I 类):长文件名(45+ 字符 ≈ 324px > 卡内 ~276px)溢出行被卡片 overflow-hidden 裁尾;预览头部文件名同样无 min-w-0, | 文件名 span 加 min-w-0 truncate;预览头返回钮加 shrink-0、名称 span 加  |
| `src/views/NsResourceQuotaDetail.vue:146` | P1 | 同 NsPVCDetail 头行破绽:无 flex-wrap/min-w-0/truncate,典型配额名(如 my-resource-quota 17 字符 ≈ 290px)+ 图标 80px + 双钮 180px ≈ | flex-wrap gap-sm + 名称块 min-w-0 truncate(NsServiceDetail |
| `src/views/NsResourceQuotaDetail.vue:186` | P2 | 左块友好名+裸 key 均不可断行无 truncate:persistentvolumeclaims 条目 = 'PersistentVolumeClaims'(170px) + '(persistentvolumecl | max-sm 隐藏裸 mono key(挪到 title 属性)或标题行 max-sm 改 flex-col  |

#### B5:集群域详情+共享件+PodDetail(29 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/views/WorkloadDetail.vue:61` | P1 | 与 PodDetail 同型但更旧:右钮组连 max-sm:flex-wrap 都没有,「删除」(88px)+「重启」(96px)+h2 长名段 min-content 合计 ~390px > 326px,重启按钮约一半 | 对齐 PodDetail Wave 2 配方:右钮组 max-sm:flex-wrap max-sm:gap- |
| `src/views/ClusterRoleBindingDetail.vue:53` | P1 | 同 ClusterRoleDetail:CRB 名(system:controller:* 常见 40+ 字符)32px 不可断行全文宽溢出 326px 内容区,页面横向滚动 | truncate 配方:中间 flex 子项 min-w-0 + h1 truncate(或 break-al |
| `src/views/ClusterRoleBindingDetail.vue:76` | P1 | 横排无 flex-wrap 无 min-w-0,ServiceAccount 主体名 system:serviceaccount:ns:name 常见 50-70 字符(mono ≈ 400-500px)溢出卡片内 27 | max-sm:flex-wrap(或 min-w-0+truncate)于 name span;namespa |
| `src/views/ClusterRoleDetail.vue:49` | P1 | 角色名无 truncate/break-all,32px/700 全文宽:内容区 326px 减图标 56px+gap 24px 后仅 ~246px,>14 字符即溢出;system:aggregate-to-admin | truncate V1V2 配方:行向链中间 flex 子项(line 48 div)加 min-w-0,h1 |
| `src/views/ClusterRoleDetail.vue:92` | P2 | system:controller:* 绑定名(40-52 字符 mono ≈ 290-374px)无 truncate/min-w-0,溢出卡片内 278px,subjects 计数被推出 | min-w-0 + truncate 于 name span,计数 span 加 shrink-0 |
| `src/views/CrdDetail.vue:183` | P1 | 检查单 H+I:长 CRD 名(如 certificaterequests.cert-manager.io 最长不可断段 23ch×14.4px≈331px;kafkatopics.kafka.strimzi.io 无连 | h1 加 break-all(同页 FULL NAME 卡 line 258 已有)+min-w-0 链;ma |
| `src/views/CrdDetail.vue:233` | P2 | 检查单 B+I:2 列无断点变体下每卡 content ≈143px,长 API group(gateway.networking.k8s.io 26ch≈187px、internal.autoscaling.k8s.i | 值 p 统一 break-all(对齐 line 258) |
| `src/views/CrdDetail.vue:313` | P0 | 检查单 D+N:未走 DataTable 卡片模式且无 overflow-x-auto。5 列 px-md 填充+图标+mono 名的最小内容宽 ≈450-600px >> 324px 可用,表格右溢被卡片 overfl | 最低配 overflow-x-auto 包裹;正解 = 迁 DataTable(手机卡片模式已内建 kv 行渲 |
| `src/views/IngressClassDetail.vue:166` | P1 | 390px 下内容宽仅 326px:左块 floor = 图标56+gap24+meta行(controller mono 不可断词如 k8s.io/ingress-nginx ~137px + DEFAULT 徽标 + | max-sm:flex-col max-sm:items-start 让动作组落到标题下方独立整行(参照 wo |
| `src/views/IngressClassDetail.vue:180` | P2 | 三 tab 合计(px-xl 64px/钮 + 文案)≈370-390px > 326px 且无 overflow-x-auto,尾端 yaml tab 被裁,计数位数多时更宽——YAML 编辑入口手机上呈截断状(E,可 | overflow-x-auto + tab 钮 shrink-0(或 px-xl 手机降 px-md) |
| `src/views/IngressClassDetail.vue:255` | P1 | 行内 namespace/name span 为 shrink-0 不可换行(mono,如 kube-system/ingress-nginx-controller ≈260px),backends 组 ml-auto  | 行加 flex-wrap(或 max-sm 改 flex-col items-start 分三行);ns/na |
| `src/views/IngressClassDetail.vue:299` | P2 | grid-cols-3 无 sm: 断点变体,390px 全屏 Modal 下(p-lg 后 ≈342px)每输入框仅 ~108px,mono 输入 networking.k8s.io 之类 apiGroup 只能横向滚 | grid-cols-1 sm:grid-cols-3(grid 断点前缀) |
| `src/views/IngressClassDetail.vue:316` | P2 | 同族问题:手机全屏 Modal 内 ~26px 移除钮触点不足(G) | max-sm:after 四件套 |
| `src/views/NamespaceDetail.vue:79` | P2 | 页面主操作 ≈ 28px 触控目标(同行 CreateWithYamlButton 已带 max-sm:min-h-[40px],此两钮漏配,视觉高度也不齐) | max-sm:min-h-[40px] 对齐 SplitButton 先例 |
| `src/views/NamespaceDetail.vue:161` | P1 | 未走 DataTable 且无横向滚动兜底:40 字符 workload 名不可断,5 列 min-content ≈ 700px,溢出被 section 的 overflow-hidden 直接裁掉 — Replica | 外层包 overflow-x-auto;或 DataTable 卡片化(本页其余 section 已是 12  |
| `src/views/NodeDetail.vue:91` | P1 | 头部元信息横排(StatusChip+IP+os·kernel+roles+arch+runtime+CORDONED)无 flex-wrap 无 min-w-0,min-content 约 430px+,叠加右侧 Co | max-sm:flex-wrap(元信息行)+ 外层 max-sm:flex-col 让按钮组掉行;文本容器补 |
| `src/views/NodeDetail.vue:134` | P2 | grid-cols-3 无 sm:/md: 断点变体,390px 下每列仅 ~98px,'31.3 Gi / 62.6 Gi' 等 mono 用量串折成多行、ProgressBar show-label 挤压(同页其他网 | grid 断点前缀:grid-cols-1 sm:grid-cols-3(对齐 ClusterOverview |
| `src/views/PVDetail.vue:97` | P1 | 326px 内容宽下:左块 floor(meta 行 StatusChip+Capacity+Age ≈230 + 图标80)≈310px,右组两钮 min≈100-120px,合计超宽 ~90px;即便修掉 t() 字 | max-sm:flex-col 堆叠动作组(44px 止血条配方);与上一条 t() 插值修复联动 |
| `src/views/PVDetail.vue:99` | P1 | 7 处 t('common.xxx') 未包 {{ }} 插值,Vue 模板按纯文本渲染,线上按钮/弹窗标题直接显示 t('common.edit')、t('common.delete') 等字面量(已在 dist/as | 补 {{ t('common.edit') }} 插值(对照同族 StorageClassDetail.vue |
| `src/views/PVDetail.vue:177` | P2 | 手机档 Modal 全屏,删除某行 label/annotation 需点 ~26px 的 X 钮(p-xs+text-base 图标),紧邻输入框极易误触输入框(G) | max-sm:after 四件套 |
| `src/views/PodDetail.vue:175` | P2 | 横向双 pane 手机无自动收起/切换:326px 下左树只分得 ~104px(文件名几乎只剩图标),拖把 w-1(4px)触控极难命中;虽有右上角方向切换钮(手动竖切+localStorage 持久)可自救,但默认进入 | isPhone 时 defaultDirection 强制 vertical(或树默认收起+面包屑导航);拖把 |
| `src/views/PodDetail.vue:221` | P1 | 外层 row 无 wrap、左列无 min-w-0、h2 display-lg 无 truncate:pod 名哈希段(如 7f9c8c6f5a)在 36px 字号下 ~190px,加上已 wrap 的右侧钮组最宽 ~1 | h2 补 truncate V1 配方(列向容器内 truncate 元素带 min-w-0/w-full)+ |
| `src/views/PodDetail.vue:259` | P1 | 5 个 tab 每个 px-xl(48px)+图标+文字 ≈110-136px,合计 540-680px >> 326px 可用宽;父卡片(col-span-12 lg:col-span-9 ... overflow-h | tab 条加 overflow-x-auto(横向可滚)或手机紧凑化:max-sm:px-md、label m |
| `src/views/PodDetail.vue:287` | P1 | 单行 flex 无 wrap 无 overflow-x-auto:容器 label+select(font-mono,选项为容器名不可缩)+模式说明文本+Exec/Attach 两钮+debug 钮合计远超 326px, | flex-wrap + 模式说明加 max-sm:hidden;或 overflow-x-auto;selec |
| `src/views/StorageClassDetail.vue:97` | P1 | 同款页头:左块 floor(meta 行 provisioner mono 不可断词 ~151px + 图标80)≈257px,右组三钮 min≈185px,合计≈442px > 326px;编辑与删除两钮整体落在可视区 | max-sm:flex-col 堆叠 + 动作组独立行(44px 止血条/workload 动作条配方) |
| `src/views/StorageClassDetail.vue:124` | P2 | grid-cols-2 在手机每轨仅 ~131px;provisioner 是点分不可断 mono token(rancher.io/local-path / kubernetes.io/aws-ebs ≈137-151 | 值元素补 break-all(同 IC/RC 详情 kv 配方);或 grid-cols-1 sm:grid- |
| `src/views/StorageClassDetail.vue:170` | P2 | 同 PVDetail:手机全屏 Modal 内 ~26px 移除钮,触控目标不足(G) | max-sm:after 四件套 |
| `src/views/WorkloadDetail.vue:125` | P2 | 未走 DataTable(手机卡片模式)的裸表:5 列 × px-lg(32px/列) 仅内边距就 160px,加 mono pod 名/node 名内容 min-content 合计 ~360-390px > 294p | 换 DataTable 卡片化(同页其它列表既定方案);或外卡改 overflow-x-auto 包 tabl |
| `src/components/common/ResourceReferences.vue:70` | P1 | D: 五列(detail 列含 mono 不可断串如 ENV ← ConfigMap.key)min-content ≈600px+,外层卡 overflow-hidden 硬裁无横滚,手机上 Detail/Status | overflow-x-auto 包裹或 DataTable 卡片化 |

#### B6:SSH 终端手机配件 R7(1 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/components/ssh/SshTerminal.vue:213` | P1 | SSH 终端(浮窗 SshTerminalWindow + 独立页 /ssh-terminal-popup)无 isPhone 虚拟按键条也无字号热调:手机软键盘发不出 Esc/Tab/Ctrl+C/方向键,vim/to | 照搬 InteractiveTerminal.vue:283 配方:v-if=isPhone 的 overfl |

#### B7:任务栏+workbench 壳(11 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/components/terminal/TerminalTaskbar.vue:219` | P2 | 整条任务栏交互目标高 ~16-24px,仅各 chip 行内 ×(234/246/257/270)拿到 max-sm:min-h-[40px] max-sm:min-w-[40px] 补偿;手机上恢复终端的主入口(chi | 参照同文件 × 的 max-sm:min-h-[40px] 配方扩到 chip 本体/closeAll/+(+ |
| `src/components/terminal/TerminalTaskbar.vue:311` | P1 | SSH 分组 chip 通常在任务栏右半屏,390px 视口下 left≈250px 的 200px+(行内容无 max-width,长 label 可撑到 300px+)菜单右缘越出屏外,行内关闭/改名钮不可达;同文件 | left 钳制: style left 改 min(menuLeft, 容器宽-菜单宽-8),或 max-sm |
| `src/components/terminal/TerminalTaskbar.vue:320` | P2 | 触控目标 ~17px(2px padding+13px icon)且无四件套;紧邻的 317 行改名 span 同尺寸却带 relative max-sm:after:absolute max-sm:after:-ins | 补 max-sm:after 四件套(与 317 行 edit 同款) |
| `src/components/workbench/WorkbenchChat.vue:1481` | P2 | w-8 h-8(32px) 是对话页最高频主操作,低于 40px 触控标准且无命中区扩展(G 类) | max-sm:w-10 max-sm:h-10 或 max-sm:after 四件套 |
| `src/views/WorkbenchLedger.vue:102` | P1 | 父级虽有 flex-wrap,但控件组本身是一个不可换行的 flex 子项:select min-w-[180px] + 两个按钮 ≈427px > 手机可用 ~294px,整组横向溢出,蒸馏/Bootstrap 按钮要 | 控件组内加 flex-wrap + 子项 max-sm:w-full,或 max-sm:flex-col |
| `src/views/WorkbenchLedger.vue:167` | P2 | grid-cols-2 无断点前缀:手机全屏 Modal(common/Modal isPhone)下两栏各 ~150px,两块 mono 学习笔记并排挤爆不可读(B 类) | grid-cols-1 sm:grid-cols-2(grid 断点前缀配方) |
| `src/views/WorkbenchProjects.vue:192` | P2 | py-0.5 + text-body-xs(11px) 触控目标仅 ~22px 高,远低于 40px,且 select 不能挂伪元素无命中区扩展(G 类) | max-sm:py-sm/放大字号(select 不能用 after 四件套),或换 bottom-sheet |
| `src/views/WorkbenchRecords.vue:180` | P2 | 服务端绝对路径(如 /home/.../data/xxx.db,60+ 字符)是长 unbreakable mono 串,span 无 break-all/truncate → 溢出卡片边界(I 类) | 加 break-all 或 truncate(mono 长串配方) |
| `src/views/WorkbenchRecords.vue:193` | P1 | shrink-0 项合计 ~250-270px(status chip mono、projectName 无上限、'N💬 N↻' 计数、w-14 时间),手机行宽 ~294px,flex-1 truncate 的预览(该 | 次要列 max-sm:hidden(计数/时间),projectName 去 shrink-0 加 trunc |
| `src/views/WorkbenchRecords.vue:221` | P2 | ts w-24(96px) + 工具名 shrink-0 不可断(apply_project_manifests 等 ≈120-152px mono)+ badge + 结果,固定项 ~320-360px > ~294p | 工具名去 shrink-0 加 truncate min-w-0;时间戳 max-sm:hidden 或行改两 |
| `src/views/WorkbenchShell.vue:56` | P1 | flex 无 wrap 无 overflow-x-auto,且 WbStage 根 overflow-hidden 硬裁:中文档四 tab≈376px>356px 靠 CJK 逐字换行挤下;英文档(Projects/Se | tab 行加 overflow-x-auto(既有配方);或 max-sm 下收成图标-only/两行 gri |

#### B8:列表页头/tab/actions 槽(44 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/views/ClusterCerts.vue:173` | P2 | py-0.5+text-xs 仅 ~20px 高的过滤 pill,触控目标远低于 40px 且无命中区扩展 | max-sm:after 四件套(参考 Nodes.vue:83 / DataTable.vue:148 既有 |
| `src/views/ClusterResourceList.vue:191` | P1 | MutatingWebhookConfigurations/ValidatingWebhookConfigurations 是单个不可断单词(29/31 字符 24px ≈ 380-415px),加右侧刷新钮 ~90px | max-sm:flex-col max-sm:items-start 收头行 + h2 max-sm 缩字号/ |
| `src/views/ClusterResourceList.vue:196` | P2 | py-1.5(12px)+ body-sm 行高 16px ≈ 28px 触控目标,低于 40px 且无扩区 | max-sm:min-h-[40px](仓库既有先例:SplitButton line 37)或 max-sm |
| `src/views/ClusterResourceList.vue:225` | P2 | 4px padding + 16px 图标 ≈ 24px 触控目标,无 max-sm:after 四件套 — 手机上破坏性操作(删 APIService/Webhook)命中区过小 | max-sm:after 四件套(relative + max-sm:after:absolute + max |
| `src/views/Configuration.vue:128` | P1 | 五个 tab 合计 ~477px >> 326px(ResourceQuotas 单 tab 129px),无 overflow-x-auto 无 wrap;手机上 LimitRanges/HPA 两个 tab 初始截断 | tab 条 overflow-x-auto + shrink-0(与 Network/Storage 同修) |
| `src/views/CrdList.vue:87` | P2 | 检查单 H+I:自定义槽丢掉默认 truncate;长 CRD 名(globalrbacdefinitions.rbacmanager.rekuberate.io 48ch≈346px > 卡内 ≈268px)溢出被硬裁 | router-link 加 truncate(继承 V1V2 配方) |
| `src/views/IngressClasses.vue:85` | P1 | 右侧两钮『部署控制器』+『创建 IngressClass』min 合计≈230px,左标题 min-content(Ingress 词 ≈120px)合计≈360-440px > 326px(en 更宽);创建钮尾部被裁 | max-sm:flex-col:两钮换行平铺(或只留主 CTA『创建』,部署入口收进 ⋯/详情),参照 44p |
| `src/views/IngressClasses.vue:118` | P2 | 手机卡片模式下这三颗 p-xs 钮(~28px)是列表内设默认/删除唯一入口的触点,无 max-sm:after 命中区扩展,触控目标 <40px(G;行点击可进详情、详情页有同款操作,故降为打磨) | max-sm:after 四件套(relative+after:absolute+after:-inset-2 |
| `src/views/NamespaceOverview.vue:244` | P2 | 检查项H/I:28px 粗体 ns 名不可断词且无 truncate,左侧最小宽(icon 48+gap 16+名字)加上右侧 调整分组+部署 SplitButton 约 134px,ns≥12 字符(kube-syst | truncate 配方 + flex-wrap:标题链 min-w-0+truncate,右侧组补 max-s |
| `src/views/NamespaceOverview.vue:338` | P2 | 检查项G:这两枚小按钮是概览卡跳转 Service/Ingress 详情的唯一入口,高度仅约 26px 且无命中区扩展 | max-sm:after 四件套 |
| `src/views/Network.vue:79` | P1 | 四个 tab 标签实测 428px(Services 94+Ingress 84+Endpoints 104+NetworkPolicies 134)>> 326px,flex 无 wrap 无 overflow-x-a | tab 条加 overflow-x-auto(仓库既有配方);可选 shrink-0 防压缩 |
| `src/views/NsHPA.vue:157` | P2 | actions 列在 DataTable 手机卡片模式下渲染为『Actions』键值行,两颗图标钮(p-xs=4px + text-sm 图标 ≈ 24-28px 命中区)无 max-sm:after 四件套,删除为破坏 | max-sm:after 四件套(relative + max-sm:after:absolute + max |
| `src/views/NsIngress.vue:241` | P2 | p-xs(4px)+text-lg 图标 ≈26px 触控目标,无 max-sm:after 四件套;手机卡片模式下 actions 渲染为 kv 行,两个 26px 钮相邻易误触且难点中 | 补 max-sm:after 四件套(relative + max-sm:after:absolute + m |
| `src/views/NsIngress.vue:326` | P1 | AnnotationKeySelect 是原生 select 且页面传的 field-class 无 w-full/flex 项 min-w-0,其内在宽由最长 option『suffix — 中文描述』(如 white | 行加 max-sm:flex-col 堆叠(AnnotationKeySelect/value 各占整行)或给 |
| `src/views/NsLimitRanges.vue:147` | P2 | 同 NsResourceQuotas 行钮:text-sm 图标 + p-xs ≈ 22px 触控目标,无 max-sm:after 命中区,delete 误触风险 | max-sm:after 四件套 + 图标升 text-lg |
| `src/views/NsNetworkPolicies.vue:102` | P2 | py-1.5(6px)→ ≈28px 高的页头主操作,无 max-sm:min-h-[40px];同页 SplitButton 类按钮均已带 max-sm:min-h,此页未跟 | 补 max-sm:min-h-[40px](SplitButton.vue:37 同款) |
| `src/views/NsNetworkPolicies.vue:108` | P2 | 无 overflow-x-auto/flex-wrap:自然宽 zh ≈410px、en(Ingress Only…)≈450px > 326px → 「双向/Both」tab 被裁;flex 强缩下 CJK 标签逐字竖 | tab 条 overflow-x-auto + 子项 whitespace-nowrap,或 flex-wra |
| `src/views/NsNetworkPolicies.vue:161` | P2 | 同 NsIngress actions:p-xs ≈26px 触控目标无四件套,手机卡片模式 kv 行内难命中且 delete 为危险操作 | 补 max-sm:after 四件套 |
| `src/views/NsPDBs.vue:186` | P2 | 与 NsHPA 同型:手机卡片模式下 actions 键值行里两颗 ~24-28px 图标钮无命中区扩展,删除操作触控目标过小(本页 139 行搜索清除钮已带四件套,同页不一致) | max-sm:after 四件套,照本页 139 行清除钮的既有写法 |
| `src/views/NsPods.vue:142` | P2 | 检查项C:四个控件自身最小宽合计约 400px(zh;en 更宽),两 层 flex 均无 flex-wrap,超 326px 可用宽,标题被挤压、Create Pod 主按钮被推出屏外 | flex-wrap:外层加 flex-wrap,右侧组 max-sm:flex-wrap;或按钮 max-sm |
| `src/views/NsResourceQuotas.vue:126` | P2 | 无 flex-wrap:h2 'ResourceQuotas'(24px ≈ 175px) + '新建 ResourceQuota' SplitButton(≈ 215px) ≈ 390px > 326px,SplitB | 头行加 flex-wrap gap-sm(或 max-sm:flex-col items-start),标题块 |
| `src/views/NsResourceQuotas.vue:176` | P2 | 手机卡片模式下动作钮仅 text-sm 图标(14px) + p-xs(4px) ≈ 22px 见方,远低于 40px 触控标准且无命中区扩展,delete 破坏性操作紧邻 open 钮易误触 | max-sm:after 四件套 + 图标升 text-lg(与 NsStorage 行钮对齐) |
| `src/views/NsServices.vue:213` | P2 | 无 flex-wrap/gap:右侧按钮组 min-content ≈110px(Endpoints,px-3 py-1.5)+8+183px(SplitButton「创建 Services」+箭头)≈300px,加左侧 | 父容器加 flex-wrap gap-sm(参照 NsServiceDetail.vue:443 同页族页头配 |
| `src/views/NsServices.vue:262` | P2 | 四处列表页自定义 name slot 均无 truncate/min-w-0(仅 icon shrink-0 + 名文本):手机卡片模式标题区外层虽有 min-w-0,但 slot 内长 DNS 名(63 字符上限,如  | name 文本统一加 truncate(truncate 直接长在 flex 子项上即归零 min-width |
| `src/views/NsServices.vue:356` | P1 | 两个 flex-1 输入(px-sm py-sm text-body-sm font-mono 内在宽 ≈162px each)无 min-w-0 无 wrap:行 min-content ≈369px > 手机全屏 M | 行加 max-sm:flex-col(或 flex-wrap+min-w-0);同型行也出现在 NsServi |
| `src/views/NsStorage.vue:201` | P2 | 手机卡片模式下 actions 仍渲染为 kv 行,两颗图标钮仅 24+8=32px 见方且彼此相邻,delete 为破坏性操作,误触率高(G 类:无 max-sm:after 命中区) | max-sm:after 四件套:relative + max-sm:after:absolute + max |
| `src/views/NsWorkloads.vue:145` | P1 | 检查项B:grid-cols-4 无 sm: 变体,390px 下每格仅约 75px,而 icon(20)+gap(8)+英文标签('StatefulSets'≈88px)+ml-auto 计数最小宽约 136px;格子 | grid 断点前缀:改 grid-cols-2 sm:grid-cols-4(或 max-sm:grid-co |
| `src/views/NsWorkloads.vue:204` | P2 | 检查项I:手机卡片里长镜像名(harbor 域名/路径 45+ 字符≈324px)超出可用宽,被 overflow-hidden 无省略号地拦腰截断,看起来像渲染残缺而非截断提示 | truncate:槽内容 span 加 truncate(+title 全文),与 name 槽(#name  |
| `src/views/PriorityClasses.vue:101` | P2 | zh 标题『优先级类 (PriorityClasses)』的不可断词 (PriorityClasses) 在 32px display 字号下 min-content ≈260px,加创建钮(icon+『Create P | max-sm:flex-col max-sm:items-start(标题与钮上下两行),或标题字号手机降档 |
| `src/views/PriorityClasses.vue:154` | P1 | 本页 DataTable 未挂 @row-click(对照 IngressClasses/RuntimeClasses 均有)→ 手机卡片模式点卡片无任何跳转,进详情只能靠 ~28px 的 open_in_new 小钮; | DataTable 挂 @row-click=openDetail(同 IngressClasses 配方)+ |
| `src/views/RBAC.vue:108` | P2 | 检查单 C:无 flex-wrap;标题侧副标题单行 ≈271px + 按钮侧 ≈216px 互挤 → 手机上按钮文字折行(「权限\n模拟」「Create\nRole」)与多行副标题并排,items-end 底对齐明显错 | 加 flex-wrap(对齐 CrdList.vue:51 既有写法),或 max-sm:flex-col |
| `src/views/RBAC.vue:123` | P1 | 检查单 E:zh/en 标签同为单个不可断行英文词,三钮最小内容宽 ≈79+166+141+gaps ≈394px > 326px 可用,无 flex-wrap 无 overflow-x-auto → ServiceAc | overflow-x-auto(+子项 shrink-0)横向滚动 tab 条,或 max-sm 换 flex |
| `src/views/RuntimeClasses.vue:68` | P1 | zh 标题『运行时类 (RuntimeClasses)』不可断词 (RuntimeClasses) min-content ≈265px + 创建钮 ≈180px,合计≈445px > 326px → 创建钮被裁剩一条( | max-sm:flex-col max-sm:items-start 两行布局 |
| `src/views/RuntimeClasses.vue:90` | P2 | 手机卡片模式删除钮 ~28px 触点,无命中区扩展(G;行点击进详情后页头有删除,故打磨级) | max-sm:after 四件套 |
| `src/views/Storage.vue:184` | P1 | tab 标签是全仓最长的三条:合计 ~470px >> 326px,无 overflow-x-auto 无 wrap;手机上 StorageClasses tab(PV/SC 是本页两个核心 tab)初始不可见,需横向拖 | tab 条 overflow-x-auto + shrink-0;可选标签缩短(PVC/PV/SC) |
| `src/views/Storage.vue:373` | P2 | 同 DeployApp KV 行根因:双 flex-1 input intrinsic min ≈212px/个,行 min-content ~460px > 手机全屏 Modal 内容宽 ~326px;Modal bo | 行加 flex-wrap 或 max-sm:flex-col;input 补 min-w-0 |
| `src/views/Workloads.vue:128` | P1 | zh 下「工作负载」(display-lg 144px min-content)+Export(~88px)+新建工作负载 SplitButton(~130px)+gap 合计 ~370px > 326px,英文 loc | 右钮组 max-sm:flex-wrap(PodDetail 同配方);或 Export 收进 SplitBu |
| `src/views/NsConfigMaps.vue:109` | P1 | C: 行无 flex-wrap(同页 NsSecrets.vue:127 已有 flex-wrap),勾选行后批量面板(文案+删除+取消 ≈190-250px)+搜索框 min-content(≈173px)+计数超 3 | flex-wrap(照抄 NsSecrets 过滤行配方) |
| `src/views/NsConfigMaps.vue:144` | P2 | G: p-xs+text-sm ≈24px 触控目标且无 after 四件套;卡片模式 kv 行同样渲染此槽(actions 列在 headers 内),手机上逐行删除/打开详情易误触 | max-sm:after 四件套(relative+after:absolute+after:-inset-2 |
| `src/views/NsRBAC.vue:264` | P2 | I: 卡片模式标题槽覆写掉了默认 truncate(206 行),ClusterRoles tab 的 system:controller:... 超长名被卡片 overflow-hidden 硬裁且无省略号,读不全(1 | name span 补 min-w-0+truncate(恢复 DataTable 默认标题配方) |
| `src/views/NsRBAC.vue:310` | P2 | G: p-xs+text-lg ≈28px 触控目标无 after 四件套,卡片模式渲染为 kv 行,误触行点击(本 tab 无 row-click,误触无害但目标过小) | max-sm:after 四件套 |
| `src/views/NsRBAC.vue:368` | P2 | B: grid-cols-3 无 sm: 变体,手机全屏弹窗内容 326px 下 kind select 仅 ~98px,ServiceAccount 选项文本被原生 select 截断难辨 | grid 断点前缀(max-sm:grid-cols-1) |
| `src/views/NsSecrets.vue:129` | P2 | G: py-xs+text-xs ≈26px 触控高 <40px 无扩展命中区,且是 Secrets 页唯一类型过滤入口,手机点选困难 | max-sm:py-sm(或 44px 止血条思路抬高 chip 高度) |
| `src/views/NsSecrets.vue:172` | P2 | G: 同 NsConfigMaps:144,卡片模式下行动作钮 ~24px 触控目标无命中区扩展 | max-sm:after 四件套 |

#### B9:创建向导/表单行 R8(6 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/views/DeployApp.vue:859` | P2 | 标题已做 hidden md:inline(好),但 6×32px 圆 + 5×(32px 线+8px margin) ≈ 392px > 326px 卡内宽:flex 收缩把 w-8 圆压成 ~21px 宽的竖椭圆(数 | max-sm: 连接线 w-4 mx-0.5、圆 w-6 h-6;或容器 overflow-x-auto |
| `src/views/DeployApp.vue:990` | P1 | flex-1 的 input 保留 intrinsic min-width(size=20 ≈170px+px-md),两输入+删除钮 min-content ≈385px;端口行更深嵌套(三层 p-md 后可用仅 ~2 | 行加 flex-wrap(参照同文件 1390 tolerations 行)+input 补 min-w-0; |
| `src/views/DeployApp.vue:1641` | P2 | 整页最关键的「下一步/部署」按钮 py-1.5 ≈32px 高 < 40px 触控下限,无 max-sm:min-h 补偿;软键盘弹起时误触率高(取消与部署相邻仅 gap-sm) | 44px 止血条配方:max-sm:min-h-[44px](参照 PodDetail 底部条/审批大按钮) |
| `src/views/admin/GroupsGrants.vue:168` | P2 | A:卡片内宽 ~260px,固定 w-40+按钮 zh 合计 ~268px 已超,en(New Group)超 ~64px,靠按钮文字折两行勉强塞下,输入框宽度不随屏折叠 | max-sm 下改 flex-wrap + 输入框 w-full(或 flex-1 min-w-0) |
| `src/views/admin/GroupsGrants.vue:176` | P2 | 行内容区 ~226px,两个计数 span(成员 N/授权 N≈64px)+两按钮+图标+5 个 gap 占 ~168px,组名仅剩 ~60px,platform-admins 类名字被截到 6-7 字符;主标识被次要计 | 计数 span 加 max-sm:hidden(或行改 max-sm:flex-col 双行),把宽度还给 t |
| `src/views/admin/GroupsGrants.vue:187` | P2 | G:图标钮仅 ~24px 高、无命中区扩展;同文件 181/182/191/226 行同族按钮都带四件套,这两处漏配 | max-sm:after 四件套补齐 |

#### B10:admin 页+杂项组件(10 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/components/common/DeployIngressControllerDialog.vue:139` | P2 | 手机全屏 Modal 下 grid-cols-2 每卡仅 ~163px,卡内标题/版本/描述/备注全挤双列,可读性差(B;文字可换行不至不可用) | grid-cols-1 sm:grid-cols-2 |
| `src/components/common/DeployIngressControllerDialog.vue:151` | P2 | 纯 text-body-sm 文字钮无内边距,触高 ~16px,远低于 40px 触控标准(G) | 加 py-sm + relative max-sm:after 四件套 |
| `src/components/common/NsAllowlistEditor.vue:57` | P2 | G:× 字符 ~16px 见方且是移除额外 ns 的唯一路径,手机上几乎点不中 | × 钮改块级并加内边距(px-1.5 py-0.5 起,目标 ≥28-32px) |
| `src/components/common/ToolOverrideEditor.vue:32` | P2 | G:11px 字号+py-0.5 → ~20px 高触控目标,deny 是关掉工具的实质操作,手机上密集难命中(仅 ApiKeyManagement 消费) | 加大内边距至 ≥32px 高(max-sm:py-2),四件套 inset 在密排 chip 上会互相重叠,用 |
| `src/views/admin/AiBehaviorConfig.vue:204` | P1 | H+I:行内容区 ~226px,固定项=开关36+工具名+审批徽章42;最长名 apply_project_manifests(23字符≈166px,requiresApproval:true)合计 ~260px,超出  | 工具名 span 补 min-w-0 truncate(与行尾描述同套 truncate V2 配方),或允许 |
| `src/views/admin/ApiKeyManagement.vue:199` | P1 | H+I+N:值区仅 ~174px,健康点+mono ns/name(如 kube-system/aliangboard-smoke ≈191px)+代管徽章+修复链接+SSH 开关 nowrap 一行,尾部被 DataT | 单元格改 flex-wrap + mono 名加 min-w-0 truncate(truncate V2 配 |
| `src/views/admin/ApiKeyManagement.vue:228` | P2 | B:手机 Modal 全屏后每列仅 ~167px,输入框 px-md 后内容区 ~135px 放 mono SA namespace/SA 名(placeholder aliangboard-smoke 已贴满),标签折 | grid 断点前缀:grid-cols-1 sm:grid-cols-2 |
| `src/views/admin/AuditTrail.vue:67` | P1 | C:zh 右侧按钮组(校验链完整性≈104px+链完整✓+刷新)占 ~231px,标题 MCP / API-key 调用审计 被压缩到 ~63px 宽(headline-lg 28px),折成 5-6 行窄柱;按钮内部也 | 头行 flex-wrap(或 max-sm:flex-col),标题占满整行、按钮组换行置底 |
| `src/views/admin/LlmConfig.vue:110` | P2 | B:手机仍 3 列,每列仅 ~76px,baseURL/model 的 mono break-all 文本(https://api.deepseek.com/v1)被拆成逐字竖排 3-4 行,来源徽章再挤一行,明显挤爆; | grid 断点前缀:grid-cols-1 sm:grid-cols-3 |
| `src/views/admin/LlmConfig.vue:148` | P1 | C:三钮合计 zh≈316px > 卡片内宽 ~262px,zh 下按钮文字折成两行(检测思考能/力);en(Save/Test Connection/Probe Reasoning)最小宽 ~317px 直接溢出,检测 | 操作行 flex-wrap,长钮换行到第二行 |

#### B11:Settings/用户中心/popups/杂项长尾(26 条)

| 位置 | 级 | 问题 | 配方提示 |
|---|---|---|---|
| `src/views/LogPopup.vue:53` | P2 | 按钮高 ≈22px(py-0.5+16px 文案)远低于 40px 触控标准,是 /log-popup 页唯一应用内关窗入口(独立标签页无壳层);与 52 行挤爆问题叠加时可能被推出屏 | min-h-[40px] 或 max-sm:after 四件套 |
| `src/components/common/ClusterCard.vue:115` | P2 | icon-only 触控目标 28px(4px padding+20px icon)无四件套,是破坏性操作(删集群,仅 window.confirm 兜底);109 行「切换到此集群」按钮 px-3 py-1.5 ≈32 | max-sm:after 四件套(icon 钮)/或 py-1.5→py-2 + min-h-[40px] |
| `src/components/common/DataTable.vue:213` | P2 | 卡片模式唯一的展开入口(YAML 查看依赖它)触控目标仅 ~24px 且紧贴行点击区,手机上难精确点中 | max-sm:after 四件套扩区(参照同文件 line 209 checkbox 已用 -m-1 p-2. |
| `src/components/common/LocaleToggle.vue:23` | P2 | 触控目标 ≈24px 高(4px×2+16px),无命中区补偿;它是这两页(无应用壳、用户菜单不可达)唯一的语言入口——看不懂中文的用户在手机上首次触达即依赖此钮 | max-sm:after 四件套(注意 pill 容器 overflow 不裁切 after)或 max-sm |
| `src/components/common/PortForwardPanel.vue:97` | P2 | 长资源名无 truncate(flex-1 min-w-0 内 p.font-mono 直接排长名,溢出压右侧状态徽章);行尾 open_in_new/stop_circle 两钮 p-xs ≈26px 无四件套——Ns | 名行 p 加 truncate;两钮补 max-sm:after 四件套 |
| `src/components/userCenter/SecuritySection.vue:297` | P2 | G:16px 图标+8px 内边距=24px 触控目标,无命中区扩展;同页 UserManagement 的同型按钮均带四件套,此处漏配 | max-sm:after 四件套:relative max-sm:after:absolute max-sm: |
| `src/components/userCenter/TokensSection.vue:90` | P2 | G:24px 触控目标无命中区扩展,吊销是本行唯一操作且带确认弹窗,手机上难点中 | max-sm:after 四件套(同 UserManagement 行内图标钮配方) |
| `src/views/AuditLogs.vue:98` | P2 | 检查单 B:无断点前缀,390px 下每卡仅 ≈103px(内 content ≈71px),标签行(图标16+gap8+「事件总数」48=72px)必折行、卡片挤压明显;能用但难看 | grid-cols-3 → 加 sm: 变体(手机 1 列或收窄 p-md/max-sm:p-xs) |
| `src/views/AuditLogs.vue:151` | P2 | 检查单 I:whitespace-nowrap 的 mono 长值(Pod/cert-manager-5d8f9b6c5-x2k9z ≈230px+)超出卡片 kv 值列(≈204px)被 overflow-hidden | 去 nowrap 改 truncate(卡片值列已 min-w-0) |
| `src/views/ClusterEvents.vue:58` | P2 | py-xs(4px)+text-xs 仅 ~24px 高,触控目标低于 40px 且无四件套;整行 flex-wrap 本身没问题 | max-sm:after 四件套 |
| `src/views/ClusterEvents.vue:89` | P2 | 手机卡片模式下 kv 值容器是 min-w-0 overflow-hidden,消息内的不可断长串(sha256/镜像引用)被静默裁断且无省略号,消息尾部内容丢失(桌面档有 overflow-x-auto 不受影响) | 插槽 span 加 break-words;与 ClusterOverview 侧栏同配方 |
| `src/views/ClusterOverview.vue:253` | P2 | 事件 message 为 K8s 原文未截断(mapEvent 直传),常见含 sha256 摘要/registry 长引用等 64+ 字符不可断串(~380px+),p 无 break-words/truncate,文 | 消息 p 加 break-words(overflow-wrap);与 ClusterEvents 列同修 |
| `src/views/Clusters.vue:71` | P2 | 按钮高 ≈32px(6px×2+20px 行高)<40px 触控标准,无 max-sm 命中区补偿;同页搜索框清除钮(89 行)已带四件套,本组漏网 | max-sm:min-h-[40px] 或 max-sm:after 四件套(对齐 89 行清除钮配方) |
| `src/views/LogPopup.vue:52` | P2 | flex 子项无 truncate/min-w-0 且外层是 style=height:36px 定高头栏:实际 ns/pod/container 串(default/nginx-ingress-controller-7 | max-sm:hidden(桌面保留全串)或补 min-w-0+truncate;关窗钮存活优先 |
| `src/views/MonitoringCenter.vue:161` | P2 | py-0.5(2px)+text-xs 行高仅 ~20px 高的切换钮,触屏命中率差且无 max-sm:after 命中区扩展,误触切错指标/过滤 | max-sm:after 四件套(relative + max-sm:after:absolute + max |
| `src/views/Namespaces.vue:143` | P2 | ≈ 28px 触控目标低于 40px,无扩区处理 | max-sm:min-h-[40px](SplitButton 先例)或 max-sm:after 四件套 |
| `src/views/Namespaces.vue:172` | P2 | 34px 触控目标且三钮仅隔 4px,删除(破坏性)与编辑紧邻 — 手机误触风险(有确认弹窗兜底故非 P1) | max-sm:after 四件套逐钮扩区(参考 DataTable 列管理 close 钮 line 148  |
| `src/views/Nodes.vue:143` | P2 | 图标钮 p-sm(~36px,低于 40px)且语义全靠 :title tooltip——触屏无 hover,手机上 lock/lock_open/output 三图标无任何文字说明,用户无法区分 cordon 与 dr | max-sm:after 四件套扩命中区;卡片模式下建议给可见文字标签(44px 止血条思路) |
| `src/views/NsEvents.vue:62` | P2 | 类型过滤是本页核心筛选控件,py-xs(4px)+text-xs(16px 行高)≈ 24px 高触控目标,无 max-sm:after 四件套,手机上连续切换过滤极易误触 | max-sm:after 四件套(需补 relative,按钮本身非定位元素) |
| `src/views/RbacCanI.vue:170` | P2 | 检查单 I:subjectName 无 break-all(datalist 恰恰提供 system:serviceaccount:kube-system:xxx 这类 54ch≈389px 不可断名),把结果卡右缘撑爆 | 该 p 加 break-all(对齐 line 175) |
| `src/views/Settings.vue:364` | P1 | 检查单 D+I:未走 DataTable 且无 overflow-x-auto;message 列为 mono 组件健康消息(etcd/scheduler 的 https://…/healthz 长 URL,不可断行)→ | overflow-x-auto 包裹;或 DataTable 卡片化(message 值列自带 min-w-0 |
| `src/views/Settings.vue:457` | P2 | 检查单 G:全部 <40px 命中区且无 max-sm:after 四件套(本三文件 grep max-sm:after:content 为 0 处,其他 views 已普遍铺设);MFA/MCP 开关是功能唯一入口,2 | max-sm:after 四件套(relative + max-sm:after:absolute + max |
| `src/views/Settings.vue:522` | P1 | bg-primary 与 text-primary 引同一 CSS 变量 → 按钮文字与背景同色,「保存」标签在手机(及桌面)上不可见,只剩一块色块(注:非视口专属,但直接命中本页手机可用性;全站其余按钮均为 bg-pr | 改 text-on-primary(对齐 RBAC.vue:117 等全站惯例) |
| `src/views/Settings.vue:541` | P1 | 检查单 A:label 固定 w-56(224px)+shrink-0,而行容器仅 ≈260px(326−卡片 p-md×2 嵌套)→ 224+8 后 input 只剩 ≈28px 或行溢出被卡片 rounded-xl  | max-sm:w-auto(或整行 max-sm:flex-col max-sm:items-start)让  |
| `src/views/Settings.vue:639` | P1 | 检查单 A 同源:w-56(224px)+shrink-0 标签把 flex-1 min-w-0 输入框压到 ≈28px 宽 → 手机上 Issuer URL/Client Secret 等 SSO 配置无法检视输入内容 | max-sm:w-auto 或 max-sm 上置 label(同上);安全 tab 全部 w-56 行统一处 |
| `src/views/UserProfile.vue:33` | P1 | E:五个 tab(资料/安全/活动/访问令牌/偏好设置)合计最小宽 zh≈444px、en≈501px,远超手机可用 ~294px;zh 下 CJK 逐字换行成竖排多行标签,en 下尾部 tab(Preferences) | tab 条加 overflow-x-auto(各 tab 补 shrink-0 whitespace-nowr |
