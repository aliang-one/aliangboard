# PVC 实际用量(已用 / 容量%)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 PVC 列表每行与 PVC 详情页显示**实际已用存储 + 占容量%**(如「3 Gi / 10 Gi · 30%」+ 进度条),数据来自 kubelet `/stats/summary`,RBAC 不可用时优雅降级。

**Architecture:** 纯前端,**不改网关**。新增 composable `usePvcUsage(namespace)`:queryFn 内①拉 ns 的 raw pods(取 PVC 卷 + nodeName)②对去重 node 调 `/api/v1/nodes/{n}/proxy/stats/summary`③按 `pvcRef`(无则按 pod+volumeName 兜底)关联回 PVC,多挂载取 max,产出 `Map<pvcName, {usedBytes, capacityBytes, percent, mounted}>` + `noStatsAccess`。核心聚合逻辑 `aggregatePvcUsage(ns, deps)` 注入 fetchers(可纯单测)。列表/详情共用同一 Vue Query 缓存。复用 `ProgressBar.vue`。

**Tech Stack:** Vue 3 `<script setup>` + `@tanstack/vue-query`(`useQuery`)+ Pinia + vue-i18n;`api.k8s(path)`(`@/api/client`)。纯逻辑 `formatBytes` 走 `scripts/test.mjs` 零依赖运行器;composable 测用 vitest。

## Global Constraints

- **零新增依赖**:复用 `@tanstack/vue-query`、`ProgressBar.vue`(`src/components/common/ProgressBar.vue`,props `value` 为 0–100 百分比)、`api.k8s`。
- **网关零改动**:全部聚合在前端完成,不碰 `server/*`。
- **i18n 门禁**:`npm run i18n:check` 必须绿,zh/en 键一一对应;新键加到 `ns.storage.*`。
- **纯逻辑优先零依赖运行器**:`formatBytes` 加进 `scripts/test.mjs`;`aggregatePvcUsage` 用 vitest。
- **降级不阻塞**:任何用量数据不可用都不影响 PVC 列表/详情其余部分;不抛错到页面。
- **`mounted` 语义 = 是否有 pod 引用该 PVC**(来自 pod spec,与 stats、与 PVC phase 无关)。
- **`DataTable` 列单元格用具名插槽** `#<header.key>`(见 `src/components/common/DataTable.vue:154`);列定义在 `src/composables/tableColumnsCore.js` 的 `TABLE_CATALOG`。

---

### Task 1: `formatBytes` 纯函数 + 零依赖测试

**Files:**
- Create: `src/utils/bytes.js`
- Modify: `scripts/test.mjs`(顶部 import 区加 import;「汇总」前加测试用例)

**Interfaces:**
- Consumes: 无(纯函数,不引 Vue)。
- Produces: `export function formatBytes(n) -> string`(`n` 为字节数;返回人类可读,二进制单位 B/Ki/Mi/Gi/Ti/Pi;`null`/`NaN`/负 → `'—'`)。Task 4、Task 5 用它渲染用量文本。

- [ ] **Step 1: 在 `scripts/test.mjs` 顶部 import 区追加**

在已有 import 列表(如 `import { workloadToForm } from '../src/composables/useWorkloadToForm.js'` 那一行附近)追加:
```javascript
import { formatBytes } from '../src/utils/bytes.js'
```

- [ ] **Step 2: 在 `scripts/test.mjs` 文件末尾「`// --- 汇总 ---`」之前追加测试用例**

```javascript
// --- PVC 用量:字节数格式化 ---
test('formatBytes: 边界与二进制单位', () => {
  assert.equal(formatBytes(0), '0 B')
  assert.equal(formatBytes(512), '512 B')
  assert.equal(formatBytes(1023), '1023 B')
  assert.equal(formatBytes(2048), '2 Ki')
  assert.equal(formatBytes(3221225472), '3.0 Gi')          // 3 GiB
  assert.equal(formatBytes(10737418240), '10 Gi')          // 10 GiB
  assert.equal(formatBytes(1649267441664), '1.5 Ti')       // 1.5 TiB
})

test('formatBytes: 非法/空值 → 占位', () => {
  assert.equal(formatBytes(null), '—')
  assert.equal(formatBytes(undefined), '—')
  assert.equal(formatBytes(NaN), '—')
  assert.equal(formatBytes(-1), '—')
})
```

- [ ] **Step 3: 运行确认失败**

Run: `node scripts/test.mjs 2>&1 | grep -A2 formatBytes | head`
Expected: FAIL(`Cannot find module '../src/utils/bytes.js'`)。

- [ ] **Step 4: 实现 `src/utils/bytes.js`**

```javascript
// 存储字节数 → 人类可读(二进制单位 B/Ki/Mi/Gi/Ti/Pi)。纯函数,不引 Vue,可零依赖测试。
// null/NaN/负 → '—'(供 UI 占位)。
const UNITS = ['B', 'Ki', 'Mi', 'Gi', 'Ti', 'Pi']

export function formatBytes(n) {
  if (n == null || !Number.isFinite(n) || n < 0) return '—'
  let i = 0
  let v = n
  while (v >= 1024 && i < UNITS.length - 1) { v /= 1024; i++ }
  if (i === 0) return `${Math.round(v)} B`
  return `${v.toFixed(v < 10 ? 1 : 0)} ${UNITS[i]}`
}
```

- [ ] **Step 5: 运行确认通过**

Run: `node scripts/test.mjs 2>&1 | tail -3`
Expected:`[test] ✓ N 用例全部通过。`(含新增 2 个 formatBytes 用例)。

- [ ] **Step 6: 语法基线 + 提交**

Run: `npm run typecheck`
```bash
git add src/utils/bytes.js scripts/test.mjs
git commit -m "feat(pvc-usage): add formatBytes pure util (bytes → human-readable)"
```

---

### Task 2: `usePvcUsage` composable + 聚合逻辑 + 测试

**Files:**
- Create: `src/composables/usePvcUsage.js`
- Test: `src/composables/__tests__/usePvcUsage.test.js`

**Interfaces:**
- Consumes: `api.k8s(path)`(`@/api/client`,透传 K8s API);`useClusterStore`(`store.currentCluster`);`useQuery`(`@tanstack/vue-query`)。
- Produces:
  - `export async function aggregatePvcUsage(ns, deps)` —— 纯聚合,`deps = { listPods: async(ns)=>podList, nodeStats: async(node)=>statsSummary }`;返回 `{ usage: Map<claimName, {usedBytes:number|null, capacityBytes:number|null, percent:number|null, mounted:boolean}>, noStatsAccess:boolean }`。
  - `export function usePvcUsage(namespace)` —— Vue Query 封装;`data.value = { usage: Map, noStatsAccess }`,key `['cluster', cid, 'pvc-usage', ns]`,`refetchInterval 60000`、`staleTime 30000`、`retry 1`、`enabled = !!ns`。Task 4、Task 5 消费。

- [ ] **Step 1: 写失败测试 `src/composables/__tests__/usePvcUsage.test.js`**

```javascript
import { test, expect } from 'vitest'
import { aggregatePvcUsage } from '../usePvcUsage'

test('aggregate: 1 pod → stats pvcRef → percent=30, mounted=true', async () => {
  const deps = {
    listPods: async () => ({ items: [{ metadata: { name: 'app' }, spec: { nodeName: 'n1', volumes: [{ name: 'data', persistentVolumeClaim: { claimName: 'pvc1' } }] } }] }),
    nodeStats: async () => ({ pods: [{ podRef: { name: 'app' }, volume: [{ name: 'data', usedBytes: 3221225472, capacityBytes: 10737418240, pvcRef: { name: 'pvc1', namespace: 'ns1' } }] }] }),
  }
  const { usage, noStatsAccess } = await aggregatePvcUsage('ns1', deps)
  expect(usage.get('pvc1')).toEqual({ usedBytes: 3221225472, capacityBytes: 10737418240, percent: 30, mounted: true })
  expect(noStatsAccess).toBe(false)
})

test('aggregate: RWX 多挂载取 max usedBytes', async () => {
  const deps = {
    listPods: async () => ({ items: [
      { metadata: { name: 'a' }, spec: { nodeName: 'n1', volumes: [{ name: 'v', persistentVolumeClaim: { claimName: 'pvc1' } }] } },
      { metadata: { name: 'b' }, spec: { nodeName: 'n1', volumes: [{ name: 'v', persistentVolumeClaim: { claimName: 'pvc1' } }] } },
    ] }),
    nodeStats: async () => ({ pods: [
      { podRef: { name: 'a' }, volume: [{ name: 'v', usedBytes: 1000, capacityBytes: 10000, pvcRef: { name: 'pvc1', namespace: 'ns1' } }] },
      { podRef: { name: 'b' }, volume: [{ name: 'v', usedBytes: 3000, capacityBytes: 10000, pvcRef: { name: 'pvc1', namespace: 'ns1' } }] },
    ] }),
  }
  const { usage } = await aggregatePvcUsage('ns1', deps)
  expect(usage.get('pvc1').usedBytes).toBe(3000)
  expect(usage.get('pvc1').percent).toBe(30)
})

test('aggregate: 无 pod 引用 → map 空,noStatsAccess=false(targetNodes 空)', async () => {
  const deps = { listPods: async () => ({ items: [] }), nodeStats: async () => ({ pods: [] }) }
  const { usage, noStatsAccess } = await aggregatePvcUsage('ns1', deps)
  expect(usage.size).toBe(0)
  expect(noStatsAccess).toBe(false)
})

test('aggregate: 全 node stats 失败 → noStatsAccess=true,mounted 仍 true,用量 null,不抛', async () => {
  const deps = {
    listPods: async () => ({ items: [{ metadata: { name: 'a' }, spec: { nodeName: 'n1', volumes: [{ name: 'v', persistentVolumeClaim: { claimName: 'pvc1' } }] } }] }),
    nodeStats: async () => { throw new Error('403') },
  }
  const { usage, noStatsAccess } = await aggregatePvcUsage('ns1', deps)
  expect(usage.get('pvc1')).toEqual({ usedBytes: null, capacityBytes: null, percent: null, mounted: true })
  expect(noStatsAccess).toBe(true)
})

test('aggregate: 无 pvcRef 时按 {podName,volumeName} 兜底匹配', async () => {
  const deps = {
    listPods: async () => ({ items: [{ metadata: { name: 'a' }, spec: { nodeName: 'n1', volumes: [{ name: 'data', persistentVolumeClaim: { claimName: 'pvc1' } }] } }] }),
    nodeStats: async () => ({ pods: [{ podRef: { name: 'a' }, volume: [{ name: 'data', usedBytes: 500, capacityBytes: 1000 }] }] }),
  }
  const { usage } = await aggregatePvcUsage('ns1', deps)
  expect(usage.get('pvc1')).toEqual({ usedBytes: 500, capacityBytes: 1000, percent: 50, mounted: true })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/composables/__tests__/usePvcUsage.test.js`
Expected: FAIL(模块不存在)。

- [ ] **Step 3: 实现 `src/composables/usePvcUsage.js`**

```javascript
import { computed, unref } from 'vue'
import { useQuery } from '@tanstack/vue-query'
import { useClusterStore } from '@/stores/cluster'
import { api } from '@/api/client'

// 按 {podName, volumeName} 在 node 的卷清单里反查 claimName(无 pvcRef 时兜底)。
function lookupClaim(list, podName, volumeName) {
  if (!list) return null
  const hit = list.find(x => x.podName === podName && x.volumeName === volumeName)
  return hit ? hit.claimName : null
}

// 纯聚合:ns 内 pod↔PVC↔kubelet stats → 每 PVC 用量。注入 fetchers,可单测(不引 Vue)。
// deps.listPods(ns) → K8s podList {items:[{metadata:{name}, spec:{nodeName, volumes:[{name, persistentVolumeClaim:{claimName}}]}}]}
// deps.nodeStats(node) → kubelet {pods:[{podRef:{name}, volume:[{name, usedBytes, capacityBytes, pvcRef:{name,namespace}}]}]}
export async function aggregatePvcUsage(ns, deps) {
  if (!ns) return { usage: new Map(), noStatsAccess: false }
  const podsRes = await deps.listPods(ns).catch(() => null)
  const pods = podsRes?.items || []

  const claimsInNs = new Set()          // 该 ns 内被 pod 引用的 PVC claimName
  const byNode = new Map()              // node → [{podName, volumeName, claimName}]
  for (const pod of pods) {
    const node = pod.spec?.nodeName
    for (const vol of (pod.spec?.volumes || [])) {
      const claim = vol?.persistentVolumeClaim?.claimName
      if (!claim) continue
      claimsInNs.add(claim)
      if (!node) continue
      if (!byNode.has(node)) byNode.set(node, [])
      byNode.get(node).push({ podName: pod.metadata?.name, volumeName: vol.name, claimName: claim })
    }
  }
  const targetNodes = [...byNode.keys()]

  const statsResults = await Promise.all(targetNodes.map(n => deps.nodeStats(n).catch(() => null)))
  const raw = new Map()                 // claim → {usedBytes, capacityBytes}(取 max)
  let failures = 0
  statsResults.forEach((s, idx) => {
    if (!s || !Array.isArray(s.pods)) { failures++; return }
    const list = byNode.get(targetNodes[idx])
    for (const p of s.pods) {
      for (const vol of (p.volume || p.volumes || [])) {
        const claim = (vol?.pvcRef?.namespace === ns) ? vol.pvcRef.name : lookupClaim(list, p.podRef?.name, vol?.name)
        if (!claim || !claimsInNs.has(claim)) continue
        const used = Number(vol.usedBytes)
        const cap = Number(vol.capacityBytes)
        const prev = raw.get(claim)
        if (!prev) raw.set(claim, { usedBytes: used, capacityBytes: cap })
        else { if (used > prev.usedBytes) prev.usedBytes = used; if (cap > prev.capacityBytes) prev.capacityBytes = cap }
      }
    }
  })

  const usage = new Map()
  for (const claim of claimsInNs) {
    const u = raw.get(claim)
    const usedBytes = u?.usedBytes ?? null
    const capacityBytes = u?.capacityBytes ?? null
    const percent = (usedBytes != null && capacityBytes) ? Math.round(usedBytes / capacityBytes * 100) : null
    usage.set(claim, { usedBytes, capacityBytes, percent, mounted: true })   // 在 claimsInNs 即「有 pod 引用」→ mounted=true
  }
  const noStatsAccess = targetNodes.length > 0 && failures === targetNodes.length
  return { usage, noStatsAccess }
}

// Vue Query 封装:列表与详情共用同一缓存(按 ns 去重)。data.value = { usage: Map, noStatsAccess }。
export function usePvcUsage(namespace) {
  const store = useClusterStore()
  const cid = computed(() => store.currentCluster || 'cluster')
  const ns = computed(() => unref(namespace) || '')
  return useQuery({
    queryKey: computed(() => ['cluster', cid.value, 'pvc-usage', ns.value]),
    queryFn: () => aggregatePvcUsage(ns.value, {
      listPods: (n) => api.k8s(`/api/v1/namespaces/${encodeURIComponent(n)}/pods`),
      nodeStats: (node) => api.k8s(`/api/v1/nodes/${encodeURIComponent(node)}/proxy/stats/summary`),
    }),
    enabled: computed(() => !!ns.value),
    refetchInterval: 60000,
    staleTime: 30000,
    retry: 1,
  })
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/composables/__tests__/usePvcUsage.test.js`
Expected: PASS(5 个用例)。

- [ ] **Step 5: 语法基线 + 提交**

Run: `npm run typecheck`
```bash
git add src/composables/usePvcUsage.js src/composables/__tests__/usePvcUsage.test.js
git commit -m "feat(pvc-usage): add usePvcUsage composable (kubelet stats aggregation, RBAC-aware)"
```

> 说明:`aggregatePvcUsage`(纯逻辑)是 TDD 焦点,已覆盖正常/RWX max/未引用/全失败/兜底匹配;`usePvcUsage` 是 useQuery 薄封装(queryFn=已测的 aggregate、key/path 显见),不单独写单测,其行为由 Task 4/5 的列表/详情集成 + 手测担保。

---

### Task 3: i18n 键(zh + en,`ns.storage.*`)

**Files:**
- Modify: `src/locales/zh.json`、`src/locales/en.json`(在 `ns.storage` 块内追加)

**Interfaces:**
- Produces:`ns.storage.{thUsed, used, usageTitle, notMounted, usageNoPermission, usageNoData}`,供 Task 4、Task 5 使用。

- [ ] **Step 1: zh.json —— `ns.storage` 块内追加**

在 `src/locales/zh.json` 的 `"storage": { ... }`(在 `ns` 下)内任一现有键后追加(注意逗号):
```json
      "thUsed": "已用",
      "used": "已用",
      "usageTitle": "存储用量",
      "notMounted": "未挂载",
      "usageNoPermission": "需要 nodes/stats 权限",
      "usageNoData": "暂无用量数据",
```

- [ ] **Step 2: en.json —— 同结构**

在 `src/locales/en.json` 的 `ns.storage` 内追加:
```json
      "thUsed": "Used",
      "used": "Used",
      "usageTitle": "Storage usage",
      "notMounted": "Not mounted",
      "usageNoPermission": "Requires nodes/stats permission",
      "usageNoData": "Usage data unavailable",
```

- [ ] **Step 3: i18n 门禁**

Run: `npm run i18n:check`
Expected: 绿(键对齐 ✓、残存中文 0、缺失 0)。另校验 JSON 可解析:
`node -e "JSON.parse(require('fs').readFileSync('src/locales/zh.json','utf8'));JSON.parse(require('fs').readFileSync('src/locales/en.json','utf8'))"`。

- [ ] **Step 4: 提交**

```bash
git add src/locales/zh.json src/locales/en.json
git commit -m "i18n: 新增 ns.storage 用量相关键(thUsed/used/usageTitle/notMounted/...)"
```

---

### Task 4: PVC 列表(NsStorage.vue)——「已用」列 + 用量条

**Files:**
- Modify: `src/composables/tableColumnsCore.js`(`TABLE_CATALOG` 的 `nsStoragePVC` columns,在 `capacity` 后插 `used`)
- Modify: `src/views/NsStorage.vue`(import + `usePvcUsage` + 行合并 + `#used` 插槽)

**Interfaces:**
- Consumes: `formatBytes`(Task 1)、`usePvcUsage`(Task 2)、`ProgressBar.vue`、i18n(Task 3)。
- Produces:PVC 列表新增「已用」列,每行渲染用量条 + 「x / y」;无数据按 `mounted`/`noStatsAccess` 降级。

- [ ] **Step 1: 列目录加 `used` 列**

在 `src/composables/tableColumnsCore.js` 的 `nsStoragePVC` 条目(约 :197)的 `columns` 数组里,在 `{ key: 'capacity', ... }` 那行之后插入:
```javascript
      { key: 'used', labelKey: 'ns.storage.thUsed', label: 'Used' },
```
> `reconcileColumns`(本文件已有,且在 `scripts/test.mjs` 测过)会把新列并入用户 localStorage 配置并默认显示;无需写迁移。完成后在浏览器确认「已用」列出现。

- [ ] **Step 2: NsStorage.vue —— import + 用量 query**

在 `<script setup>` 顶部 import 区(`useResourceList` / `useQueryClient` 附近)追加:
```javascript
import ProgressBar from '@/components/common/ProgressBar.vue'
import { usePvcUsage } from '@/composables/usePvcUsage'
import { formatBytes } from '@/utils/bytes'
```
在 `const queryClient = useQueryClient()`(约 :20)之后、或与其它 query 声明相邻处,加:
```javascript
const usageQ = usePvcUsage(route.params.namespace)
const noStatsAccess = computed(() => !!usageQ.data.value?.noStatsAccess)
```

- [ ] **Step 3: NsStorage.vue —— 行合并用量**

把 DataTable 的 `:rows="paginated"` 改为 `:rows="rowsWithUsage"`,并在 `paginated` 声明(来自 `usePagination`,约 :53)之后新增:
```javascript
const rowsWithUsage = computed(() => (paginated.value || []).map(p => {
  const u = usageQ.data.value?.usage?.get(p.name)
  return { ...p, usedBytes: u?.usedBytes ?? null, capacityBytes: u?.capacityBytes ?? null, percent: u?.percent ?? null, mounted: u?.mounted ?? false }
}))
```

- [ ] **Step 4: NsStorage.vue —— `#used` 插槽**

在 PVC 的 `<DataTable>`(约 :166)内,与现有 `#capacity`/`#status` 等插槽同级,追加:
```vue
        <template #used="{ row }">
          <div v-if="row.percent != null" class="flex flex-col gap-0.5 min-w-[84px]">
            <ProgressBar :value="row.percent" size="sm" />
            <span class="text-[10px] text-on-surface-variant font-mono">{{ formatBytes(row.usedBytes) }} / {{ formatBytes(row.capacityBytes) }}</span>
          </div>
          <span v-else-if="!row.mounted" class="text-xs text-on-surface-variant/60">{{ t('ns.storage.notMounted') }}</span>
          <span v-else :title="noStatsAccess ? t('ns.storage.usageNoPermission') : t('ns.storage.usageNoData')" class="text-xs text-on-surface-variant/60 cursor-help">—</span>
        </template>
```

- [ ] **Step 5: 校验**

Run: `npm run typecheck && npm run i18n:check && npm run build`
Expected: 全通过(build 覆盖 `.vue` 编译,确认插槽/列目录无误)。

- [ ] **Step 6: 提交**

```bash
git add src/composables/tableColumnsCore.js src/views/NsStorage.vue
git commit -m "feat(pvc-usage): PVC 列表加「已用」列(用量条 + 降级)"
```

---

### Task 5: PVC 详情(NsPVCDetail.vue)——「存储用量」卡

**Files:**
- Modify: `src/views/NsPVCDetail.vue`(import + `usePvcUsage` + overview 网格里加用量卡)

**Interfaces:**
- Consumes: `formatBytes`(Task 1)、`usePvcUsage`(Task 2)、`ProgressBar.vue`、i18n(Task 3)。
- Produces:PVC 详情 overview 新增用量卡(进度条 + 「x 已用 / y 容量(p%)」);无数据按 `mounted`/`noStatsAccess` 降级。

- [ ] **Step 1: import + 用量 query**

在 `src/views/NsPVCDetail.vue` `<script setup>` import 区(`useResourceDetail`/`StatusChip` 附近)追加:
```javascript
import ProgressBar from '@/components/common/ProgressBar.vue'
import { usePvcUsage } from '@/composables/usePvcUsage'
import { formatBytes } from '@/utils/bytes'
```
在 `pvcDetail`/`pvc` 声明(约 :25-29)之后加:
```javascript
const usageQ = usePvcUsage(route.params.namespace)
const usage = computed(() => usageQ.data.value?.usage?.get(route.params.name) || null)
const noStatsAccess = computed(() => !!usageQ.data.value?.noStatsAccess)
```

- [ ] **Step 2: overview 网格里加用量卡**

在 overview 标签页的 `grid grid-cols-2`(约 :146 起,与 `status`/`capacity` 同级的那组 `<div class="p-md rounded-lg bg-surface-container-low">` 卡片)中,追加一张卡片:
```vue
            <div class="p-md rounded-lg bg-surface-container-low">
              <p class="text-label-caps text-on-surface-variant mb-xs">{{ t('ns.storage.usageTitle') }}</p>
              <template v-if="usage && usage.percent != null">
                <ProgressBar :value="usage.percent" showLabel size="md" :label="t('ns.storage.usageTitle')" />
                <p class="text-body-sm text-on-surface-variant mt-xs font-mono">
                  {{ formatBytes(usage.usedBytes) }} {{ t('ns.storage.used') }} / {{ formatBytes(usage.capacityBytes) }} ({{ usage.percent }}%)
                </p>
              </template>
              <p v-else-if="usage && usage.mounted" class="text-body-sm text-on-surface-variant">
                {{ noStatsAccess ? t('ns.storage.usageNoPermission') : t('ns.storage.usageNoData') }}
              </p>
              <p v-else class="text-body-sm text-on-surface-variant">{{ t('ns.storage.notMounted') }}</p>
            </div>
```

- [ ] **Step 3: 校验**

Run: `npm run typecheck && npm run i18n:check && npm run build`
Expected: 全通过。

- [ ] **Step 4: 提交**

```bash
git add src/views/NsPVCDetail.vue
git commit -m "feat(pvc-usage): PVC 详情加「存储用量」卡(进度条 + 降级)"
```

---

## 全量回归(所有任务完成后)

- [ ] `node scripts/test.mjs`(formatBytes + 既有纯逻辑全过)
- [ ] `npm run test:unit`(usePvcUsage + 既有 vitest 全过)
- [ ] `npm run typecheck`(语法基线)
- [ ] `npm run i18n:check`(zh/en 键对齐、无残留中文、无缺失引用键)
- [ ] `npm run build`(.vue 编译通过)
- [ ] 手测(需连真实集群 + SA 有 `nodes/stats`):
  - PVC 列表每行出现「已用」列:有挂载 → 用量条 + 「x / y」;无挂载 → 「未挂载」;
  - 点 PVC 进详情,overview 出现「存储用量」卡(进度条 + 百分比);
  - 60s / 窗口聚焦后用量自动刷新;
  - 拿掉 SA 的 `nodes/stats`(或连到不支持的集群)→ 列表「—」(tooltip「需要 nodes/stats 权限」)、详情对应文案,**页面其它部分不受影响、不报错**;
  - Pending / 孤儿 PVC → 「未挂载」。
