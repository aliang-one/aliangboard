# can_i 工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 `can_i`(read 档):AI 行动前自检绑定 SA 的 K8s RBAC —— `SelfSubjectAccessReview`(SSAR)→ `{allowed, reason, evaluationError, queried}`。

**Architecture:** 单工具,经 `runBoundedTool`(read 档 + ns 校验 + SA token + 审计)。fn 内 POST `/apis/authorization.k8s.io/v1/selfsubjectaccessreviews`,SSAR 失败(含 SA 无 SSAR RBAC 的 403)优雅返 `evaluationError` 不抛。`can_i` 已在 `BOUNDED_TOOLS` → 不改 authorize,只加 tool 实现 + registry 条目。

**Tech Stack:** Node.js(`node:test` + `node:sqlite` + `node --check`)。零新依赖。

## Global Constraints

- **零新外部依赖**:测试 `node --test`;类型 `node --check`。
- **接线铁律**:`can_i` 经 `runBoundedTool`(authorize→ns→reserve→SA-token→fn→finalize)。
- **ns 作用域**:`namespace === keyRow.boundSA_namespace`(runBoundedTool 既有校验);can_i 只检绑定 ns 的 RBAC。
- **SSAR 失败优雅**(不抛):requestFn 抛(含 403)→ fn catch → `{allowed:false, evaluationError:'...'}`,不向调用方抛。
- **不改 `authorize.mjs`**:can_i 已在 `BOUNDED_TOOLS`;实现后 `listTools()` 含它 → read 档自动广告。
- **reason/evaluationError 截断** ≤ 300 字符(防超大)。
- **commit 风格**:`feat(mcp): …` + `Co-Authored-By: Claude <noreply@anthropic.com>`。
- **测试命令**:`node --test server/api-key-tools.test.mjs`;回归 `npm test`。

## File Structure

| 文件 | 职责 | 改动 |
|------|------|------|
| `server/api-key-tools.mjs` | runBoundedTool + tools | +`can_i` tool 方法(read 档,SSAR POST,优雅 catch) |
| `server/api-key-tools.test.mjs` | tools 测试 | +can_i 用例(happy/denied/evaluationError/SSAR-403/ns-deny/缺参) |
| `server/tool-registry.mjs` | 工具 metadata | +`can_i` registry 条目(read 档) |

---

### Task 1: `can_i` 工具(SSAR + 优雅失败 + 测试)

**Files:**
- Modify: `server/api-key-tools.mjs`(+tool)、`server/tool-registry.mjs`(+条目)
- Test: `server/api-key-tools.test.mjs`

**Interfaces:**
- Produces: tool `can_i({namespace, verb, resource, group?, name?, subresource?})` → `{allowed, reason, evaluationError, queried}`;read 档。
- Consumes: `runBoundedTool`、`requestFn`(既有)。

- [ ] **Step 1: 写失败测试** — 追加到 `server/api-key-tools.test.mjs`(沿用其 `makeDb`/`mockRequestFn`/`mintKey`/`cluster`;`base = mockRequestFn()` 包一层拦 SSAR POST,与 get_resource_yaml/list(path) 测试同构):

```js
// --- can_i(RBAC 自检 via SelfSubjectAccessReview)---
test('can_i(happy): SSAR allowed → {allowed:true},resourceAttributes 正确;read 档可调', async () => {
  const db = makeDb()
  const k = mintKey(db, { owner: 'a', clusterId: 'c1', boundSA_namespace: 'ns', boundSA_name: 'sa', tier: 'read' })
  let posted = null
  const base = mockRequestFn()
  const tools = createApiKeyTools({ db, requestFn: async (ctx, path, init = {}) => {
    if (init.method === 'POST' && path.endsWith('/selfsubjectaccessreviews')) { posted = JSON.parse(init.body); return { body: { status: { allowed: true, reason: '' } } } }
    return base(ctx, path, init)
  } })
  const out = await tools.callTool(k, cluster, 'can_i', { namespace: 'ns', verb: 'delete', resource: 'secrets', group: '' })
  assert.equal(out.allowed, true)
  assert.equal(posted.kind, 'SelfSubjectAccessReview')
  assert.equal(posted.spec.resourceAttributes.namespace, 'ns')
  assert.equal(posted.spec.resourceAttributes.verb, 'delete')
  assert.equal(posted.spec.resourceAttributes.resource, 'secrets')
  assert.equal(posted.spec.resourceAttributes.group, '')
  assert.deepEqual(out.queried, { namespace: 'ns', verb: 'delete', resource: 'secrets', group: '', name: undefined, subresource: undefined })
})
test('can_i(denied): SSAR allowed:false → {allowed:false, reason}', async () => {
  const db = makeDb()
  const k = mintKey(db, { owner: 'a', clusterId: 'c1', boundSA_namespace: 'ns', boundSA_name: 'sa', tier: 'read' })
  const base = mockRequestFn()
  const tools = createApiKeyTools({ db, requestFn: async (ctx, path, init = {}) => {
    if (init.method === 'POST' && path.endsWith('/selfsubjectaccessreviews')) return { body: { status: { allowed: false, reason: 'forbidden by RBAC' } } }
    return base(ctx, path, init)
  } })
  const out = await tools.callTool(k, cluster, 'can_i', { namespace: 'ns', verb: 'get', resource: 'pods' })
  assert.equal(out.allowed, false)
  assert.match(out.reason, /forbidden by RBAC/)
  assert.equal(out.evaluationError, null)
})
test('can_i(evaluationError): SSAR evaluationError 透传', async () => {
  const db = makeDb()
  const k = mintKey(db, { owner: 'a', clusterId: 'c1', boundSA_namespace: 'ns', boundSA_name: 'sa', tier: 'read' })
  const base = mockRequestFn()
  const tools = createApiKeyTools({ db, requestFn: async (ctx, path, init = {}) => {
    if (init.method === 'POST' && path.endsWith('/selfsubjectaccessreviews')) return { body: { status: { allowed: false, evaluationError: 'cannot evaluate' } } }
    return base(ctx, path, init)
  } })
  const out = await tools.callTool(k, cluster, 'can_i', { namespace: 'ns', verb: 'get', resource: 'pods' })
  assert.equal(out.allowed, false)
  assert.match(out.evaluationError, /cannot evaluate/)
})
test('can_i(SSAR 403): SA 无 SSAR RBAC → 优雅 evaluationError,不抛', async () => {
  const db = makeDb()
  const k = mintKey(db, { owner: 'a', clusterId: 'c1', boundSA_namespace: 'ns', boundSA_name: 'sa', tier: 'read' })
  const e403 = new Error('forbidden'); e403.status = 403
  const base = mockRequestFn()
  const tools = createApiKeyTools({ db, requestFn: async (ctx, path, init = {}) => {
    if (init.method === 'POST' && path.endsWith('/selfsubjectaccessreviews')) throw e403
    return base(ctx, path, init)
  } })
  const out = await tools.callTool(k, cluster, 'can_i', { namespace: 'ns', verb: 'get', resource: 'pods' })
  assert.equal(out.allowed, false)
  assert.match(out.evaluationError, /selfsubjectaccessreviews/)
})
test('can_i: ns 不符 → policy 拒(既有 ns 校验)', async () => {
  const db = makeDb()
  const k = mintKey(db, { owner: 'a', clusterId: 'c1', boundSA_namespace: 'ns', boundSA_name: 'sa', tier: 'read' })
  const tools = createApiKeyTools({ db, requestFn: mockRequestFn() })
  await assert.rejects(tools.callTool(k, cluster, 'can_i', { namespace: 'other', verb: 'get', resource: 'pods' }), (e) => e.reason === 'policy')
})
test('can_i: 缺 verb/resource → 报错', async () => {
  const db = makeDb()
  const k = mintKey(db, { owner: 'a', clusterId: 'c1', boundSA_namespace: 'ns', boundSA_name: 'sa', tier: 'read' })
  const tools = createApiKeyTools({ db, requestFn: mockRequestFn() })
  await assert.rejects(tools.callTool(k, cluster, 'can_i', { namespace: 'ns', resource: 'pods' }), /缺 verb/)
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test server/api-key-tools.test.mjs`
Expected: FAIL — `未知工具: can_i`(callTool 派发不到)。

- [ ] **Step 3: 实现** — `server/api-key-tools.mjs` 的 `tools` 对象内(如 `get_events` 之后)加:

```js
    can_i: async (keyRow, cluster, a, source) => runBoundedTool({
      keyRow, cluster, tool: 'can_i', source, namespace: a.namespace, verb: 'can_i', resource: `${a.verb || '?'}/${a.resource || '?'}`,
      summary: `can ${a.verb || '?'} ${a.group ? a.group + '/' : ''}${a.resource || '?'}`,
      fn: async (saCtx) => {
        if (!a.verb || !a.resource) throw new Error('can_i 缺 verb/resource(如 verb=delete resource=secrets)')
        const ra = { namespace: a.namespace, verb: a.verb, group: a.group || '', resource: a.resource }
        if (a.name) ra.name = a.name
        if (a.subresource) ra.subresource = a.subresource
        let body
        try {
          ({ body } = await requestFn(saCtx, '/apis/authorization.k8s.io/v1/selfsubjectaccessreviews', {
            method: 'POST', headers: { 'content-type': 'application/json' },
            body: JSON.stringify({ apiVersion: 'authorization.k8s.io/v1', kind: 'SelfSubjectAccessReview', spec: { resourceAttributes: ra } }),
          }))
        } catch (e) {
          // SSAR 失败(SA 无 create SSAR 的 RBAC → 403;或 5xx/网络)→ 优雅返,不抛
          const code = e.status ? `http${e.status}` : 'error'
          const hint = e.status === 403 ? 'SA 无 selfsubjectaccessreviews 的 create 权限(can_i 需 SA 有 SSAR RBAC)' : (e.message || 'error')
          return { allowed: false, reason: null, evaluationError: `SSAR 请求失败(${code}): ${String(hint).slice(0, 300)}`, queried: ra }
        }
        return {
          allowed: !!body?.status?.allowed,
          reason: body?.status?.reason ? String(body.status.reason).slice(0, 300) : null,
          evaluationError: body?.status?.evaluationError ? String(body.status.evaluationError).slice(0, 300) : null,
          queried: ra,
        }
      } }),
```

`server/tool-registry.mjs` 的 `K8S` 数组(`get_events` 条目附近)加:

```js
  { name: 'can_i', minTier: 'read', requiresApproval: false,
    description: '自检绑定 SA 的 K8s RBAC:能对 <resource> 做 <verb> 吗?(SelfSubjectAccessReview,kubectl auth can-i 语义)。返 {allowed, reason, evaluationError, queried}。行动前调用以减少被拒。SA 需有 create selfsubjectaccessreviews 的 RBAC(否则 evaluationError 提示)。只检绑定 namespace。',
    inputSchema: { type: 'object', properties: { namespace: { type: 'string' }, verb: { type: 'string', description: 'kubectl can-i 动词:get/list/watch/create/update/patch/delete/deletecollection/exec/...' }, resource: { type: 'string', description: '如 pods/deployments/ingresses/secrets' }, group: { type: 'string', description: 'apiGroup(默认空=core;如 apps/networking.k8s.io)' }, name: { type: 'string' }, subresource: { type: 'string', description: '子资源如 pods/log' } }, required: ['namespace', 'verb', 'resource'] } },
```

- [ ] **Step 4: 跑测试确认通过 + 回归**

Run: `node --test server/api-key-tools.test.mjs`
Expected: PASS(6 新用例 + 全回归)。

- [ ] **Step 5: commit**

```bash
git add server/api-key-tools.mjs server/api-key-tools.test.mjs server/tool-registry.mjs
git commit -m "feat(mcp): can_i 工具(SSAR RBAC 自检,read 档;SA 无 SSAR 权限优雅降级)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 全量验证

**Files:** 无代码改动(验证)。

- [ ] **Step 1: 全量验证**

Run: `npm test && npm run typecheck && npm run build`
Expected:
- `npm test`:所有 server `*.test.mjs` + vitest PASS(含 6 新 can_i 用例)。
- `typecheck`:`node --check` 全过。
- `build`:Vite 构建成功(本轮无前端改动,纯回归)。

- [ ] **Step 2: commit(若有调整则一并;否则跳过)**

---

## Self-Review(写完后自查,已修正)

**1. Spec 覆盖:**
- can_i(SSAR,优雅 403,ns-bound,read 档,不改 authorize)→ Task 1 ✓
- 全量验证 → Task 2 ✓
- 不做 policy 合取 / cross-ns / patch → 非 target,无 task ✓

**2. 类型/签名一致性:**
- `can_i` 经 `runBoundedTool({...,source})`(source 是既有形参)✓
- 返回 shape `{allowed, reason, evaluationError, queried}` Task 1 定义、测试一致 ✓
- `ra`(resourceAttributes)与 SSAR body + `queried` 一致 ✓

**3. 无占位:** 各步含可执行代码/命令;SSAR body 完整;6 个测试覆盖 happy/denied/evalError/403/ns/缺参。

**4. 已知简化(非占位):**
- `queried.name/subresource` 未传时为 `undefined`(测试 deepEqual 用 undefined);SSAR body 用 `if (a.name) ra.name=...` 仅在提供时含。
- audit `verb:'can_i'`(自定义,清晰标识是 check 而非真 delete/get)。
