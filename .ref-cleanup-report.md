# ref-cleanup report (chore/remove-dead-store-refs)

Branch: `chore/remove-dead-store-refs` @ origin/main `5549431`.

## Step B — 5 genuinely-broken spots migrated to Vue Query

| View | Before (read empty store ref) | After (Vue Query) |
|------|-------------------------------|-------------------|
| `PVDetail.vue` | `pv = store.getPVByName(name)` + secondary `sc = store.getSCByName(pv.storageClass)` | `pvDetail = useResourceDetail(['cluster',cid,'pvs',name], fetchPV)`; `sc` via cluster-wide `_scQ = useResourceList(['cluster',cid,'storageclasses'])` filtered by `pv.storageClass` (list-filter chosen over per-name detail to avoid reactive-key problem: `pv.storageClass` only known after PV loads). |
| `StorageClassDetail.vue` | `sc = store.getSCByName(name)` | `scDetail = useResourceDetail(['cluster',cid,'storageclasses',name], fetchStorageClass)`. |
| `NsServiceDetail.vue` | `ep = store.getEndpointsByName(name, ns)` | `endpointsQ = useResourceList(['cluster',cid,'endpoints'], fetchEndpoints)` + `.find(name+ns)`. |
| `NsServices.vue` | `epState` per-row `store.getEndpointsByName(row.name, row.ns)` | added `_endpointsQ` (cluster-wide) + per-row `.find()` in `epState`. |
| `Namespaces.vue` | `if (store.getNamespaceByName(name))` (dup-check) | reuses existing `namespacesQuery` → `namespaces.value.some(n => n.name === name)`. |

## Step A — removed `?? store.getXxxByName(...)` fallback tails (20 views)

Each `xxx = computed(() => xxxDetail.data.value ?? store.getXxxByName(...))` → `xxx = computed(() => xxxDetail.data.value)`.

Views edited (20): NsServiceDetail (svc), NsRoleDetail, PodDetail, NsNetworkPolicyDetail, NsConfigMapDetail, NamespaceDetail, NodeDetail, ClusterRoleBindingDetail, NsRoleBindingDetail, ClusterRoleDetail, NsPVCDetail, NsIngressDetail, NsPDBDetail, NsLimitRangeDetail, NsSecretDetail, NsHPADetail, PriorityClassDetail, CrdDetail, NsServiceAccountDetail, NsResourceQuotaDetail.

After Step A+B: `grep "store\.get[A-Z]\w*ByName" src/**/*.vue` → 0 callers.

## Step C — dead code removed

### Deleted `getXxxByName` function definitions (24, all zero-caller) + removed from store `return {...}`:
getWorkloadByName, getPodByName, getNodeByName, getNamespaceByName, getServiceByName, getIngressByName, getConfigMapByName, getSecretByName, getPVCByName, getNetworkPolicyByName, getHPAByName, getResourceQuotaByName, getLimitRangeByName, getRoleByName, getServiceAccountByName, getRoleBindingByName, getPVByName, getSCByName, getEndpointsByName, getClusterRoleByName, getClusterRoleBindingByName, getPDBByName, getPriorityClassByName, getCRDByName.

### Deleted empty `const xxxList = ref([])` (11, zero internal `.value` reads/writes + zero external `store.xxxList` reads):
configMapList, secretList, pvcList, saList, networkPolicyList, hpaList, resourceQuotaList, limitRangeList, pdbList, priorityClassList, crdList.

### Kept (still live — verified readers/writers exist):
- **Refs still read externally as `?? store.xxxList` fallback**: nodeList (TopNavBar), namespaceList (10 views: TopNavBar/SideNavBar/Network/DeployApp/Configuration/Workloads/Storage/RBAC/RbacCanI/CopyWorkloadDialog).
- **Refs still used internally by cluster.js**: workloadList (fetchWorkload upsert, addWorkload, getWorkloadPods, watch merge), podList (running/pending/failedPods cluster stats + getWorkloadPods), eventList (refreshEvents + watch merge), serviceList (getWorkloadReferences + watch merge), ingressList (watch merge), endpointsList (updateEndpoints writes), pvList/scList (bespoke remoteCreate/Delete + refetch), roleList (refreshRoles + getWorkloadReferences), roleBindingList + clusterRoleBindingList (getWorkloadReferences), auditLogList (logAudit).
- **`getWorkloadPods`**: kept (not a getXxxByName; does real selector logic, out of scope).
- Stale comment in NsRoleBindingDetail.vue updated; dead `getServiceByName: () => null` removed from NsIngress.unwrapping.test.js mock.

## Verify gate

- `npm run typecheck`: ✓ (132 .js/.mjs pass node --check)
- `npm run build`: ✓ (built in 27s)
- `npm run test:unit`: 486 passed / 1 failed — the 1 failure is `mount DeployApp.vue` timeout (`ECONNREFUSED 127.0.0.1:3000`), a pre-existing environmental failure (no dev server running); DeployApp.vue uses none of the removed refs (0 grep hits on both this branch and origin/main).
- `node scripts/check-await-race.mjs`: ✓
- `node scripts/check-missing-value.mjs`: ✓
