# Cleanup Report — dead store refs (chore/cleanup-dead-refs)

Branch: `chore/cleanup-dead-refs` (from origin/main `3f9fb72`)

## Task 1 — `updateNamespace`: REMOVED

Verdict: **zero callers, deleted.**
- Grep `updateNamespace` (singular) across `src/**/*.{vue,js}`: only the definition (cluster.js:1136) + the store's own `return {...}` (line 2081). No `.vue`/composable/test caller.
- The plural `updateNamespaces` hits in `api/client.js:234` + `ApiKeyManagement.vue:75` are a **different** API-key-scoped function (unrelated).
- It was pure local mutation (no remote) — dead in all-real-data world.
- Changes: deleted function body (cluster.js) + removed from `return {...}`.

## Task 2 — `getWorkloadForEdit` cold-cache probe: NO-OP (already optimal)

Verdict: **already implemented — nothing to change.**
- Current code (cluster.js:620-631) already does exactly the requested optimization:
  1. Line 623: `queryClient.getQueryData(['cluster', cid, 'workloads'])` → find by name (cache-first)
  2. Line 625: returns on hit (zero extra requests)
  3. Lines 627-629: only on cache miss does the 3-request type-probe (Deployment→StatefulSet→DaemonSet) run as fallback
- This was shipped by the bespoke-fix commit `3f9fb72` ("fix(crud): bespoke update 改 fetch-first"). The comment at line 620 confirms intent ("all-real-data 真相源；替代旧 workloadList.findIndex").
- Probe retained as fallback (cold-cache handling preserved). No edit made.

## Task 3 — orphan store-ref audit

Method: for each `const xxxList = ref([])`, grep ALL readers (`grep xxxList src/ --include=*.vue --include=*.js`, excluding declaration + return) AND writers (push/splice/assignment), plus external callers of each sibling `getXxxByName` helper (the helpers read the ref via `.value.find`).

Key finding: no `.vue` file reads any `store.xxxList` directly (all views shadow with a local `computed(() => xxxQuery.data.value || [])` reading Vue Query). The ONLY readers of most refs are the internal `getXxxByName` helpers, which detail views call as a **fallback**: `detail.data.value ?? store.getXxxByName(...)`.

| Ref | External readers (helpers/bare) | External writers | Verdict |
|-----|---------------------------------|------------------|---------|
| `configMapList` | `getConfigMapByName` ← NsConfigMapDetail.vue:29 | — | **KEPT** (read-by-helper) |
| `secretList` | `getSecretByName` ← NsSecretDetail.vue:28 | — | **KEPT** (read-by-helper) |
| `pvcList` | `getPVCByName` ← NsPVCDetail.vue:30 | — | **KEPT** (read-by-helper) |
| `serviceList` | `getServiceByName` ← NsServiceDetail.vue:39; + internal readers (line 711, 1125) | `addService` sideEffects.onAdd writes `namespaceList.services` (not serviceList) | **KEPT** (read) |
| `ingressList` | `getIngressByName` ← NsIngressDetail.vue:30; + internal (711) | — | **KEPT** (read) |
| `networkPolicyList` | `getNetworkPolicyByName` ← NsNetworkPolicyDetail.vue:26 | — | **KEPT** (read-by-helper) |
| `hpaList` | `getHPAByName` ← NsHPADetail.vue:26 | — | **KEPT** (read-by-helper) |
| `resourceQuotaList` | `getResourceQuotaByName` ← NsResourceQuotaDetail.vue:25 | — | **KEPT** (read-by-helper) |
| `limitRangeList` | `getLimitRangeByName` ← NsLimitRangeDetail.vue:24 | — | **KEPT** (read-by-helper) |
| `saList` | `getServiceAccountByName` ← NsServiceAccountDetail.vue:30 | — | **KEPT** (read-by-helper) |
| `roleBindingList` | `getRoleBindingByName` ← NsRoleBindingDetail.vue:27; + internal (1988 clusterRbac) | — | **KEPT** (read) |
| `pdbList` | `getPDBByName` ← NsPDBDetail.vue:25 | — | **KEPT** (read-by-helper) |
| `workloadList` | many internal readers (getWorkloadByName, refs, addWorkload push, etc.) | `addWorkload` push (606); test mock | **KEPT** (read + written) |
| `pvList` | `getPVByName` ← PVDetail.vue:22; `addPV`/`deletePV` use refetch | `addPV` refetch writes (553) | **KEPT** (read + written) |
| `scList` | `getSCByName` ← PVDetail.vue:33, StorageClassDetail.vue:21 | `addStorageClass` refetch writes (572) | **KEPT** (read + written) |
| `roleList` | `getRoleByName` ← NsRoleDetail.vue:25; + internal (1038, 1049, 2000) | `hydrate`/refresh writes (406) | **KEPT** (read + written) |
| `clusterRoleBindingList` | `getClusterRoleBindingByName` ← ClusterRoleBindingDetail.vue:23; + internal (1989) | — | **KEPT** (read) |
| `priorityClassList` | `getPriorityClassByName` ← PriorityClassDetail.vue:21 | — | **KEPT** (read-by-helper) |
| `endpointsList` | `getEndpointsByName` ← NsServiceDetail.vue:70, NsServices.vue:107; `updateEndpoints` exposed | — | **KEPT** (read) |
| `podList` | many internal readers (getPodByName, podsForWorkload, watch, metrics) | watch/hydrate/addPod writes | **KEPT** (read + written) |
| `nodeList` | getNodeByName; internal readers (metrics, health); `useClusterHealth` + TopNavBar.vue:81 | hydrate/metrics/healthcheck writes | **KEPT** (read + written) |
| `namespaceList` | `getNamespaceByName`; MANY `.vue` fallbacks (`nsQ.data ?? store.namespaceList`); internal (478-479, 607, 835) | hydrate writes (1270) | **KEPT** (read + written) |
| `eventList` | `eventsFor`; internal readers (977, 1245) | watch/refreshEvents writes (886, 947-950) | **KEPT** (read + written) |
| `crdList` | `getCRDByName` ← CrdDetail.vue:26 | — (never written!) | **KEPT** (read-by-helper; write-only-empty — returns undefined always, but helper is a live fallback caller) |
| `auditLogList` | none | `logAudit` writes (1298) — but `logAudit` itself has ZERO external callers | **KEPT** (write-only; suspicious — `logAudit`+`auditLogList` appear to be a dead pair, but conservative rule keeps write-only refs) |
| `ingressClassList` | **none** (`getIngressClassByName` has ZERO external callers) | none | **REMOVED** |
| `runtimeClassList` | **none** (`getRuntimeClassByName` has ZERO external callers) | none | **REMOVED** |
| `logEntries` | **none** (zero readers anywhere) | none | **REMOVED** |

### Removed (3 refs + 2 dead helpers + 1 dead function)
- `logEntries = ref([])` — zero readers, zero writers
- `ingressClassList = ref([])` + `getIngressClassByName()` — ref only read by helper; helper has zero external callers; factory CRUD for ingressclass uses `remoteDeletePath` (no store ref)
- `runtimeClassList = ref([])` + `getRuntimeClassByName()` — same as above
- `updateNamespace()` (Task 1)
- All removed symbols also dropped from the store `return {...}`

### Kept-but-notable (potential future cleanup, separate issues)
- **`crdList`**: read by `getCRDByName` (live fallback in CrdDetail.vue) but NEVER written → always returns undefined. The helper is a no-op fallback. Not removed (has a reader).
- **`auditLogList`**: written by `logAudit` but `logAudit` is never called externally, and `auditLogList` is never read. Appears to be a dead writer+ref pair. Kept per conservative "write-only → note" rule. Both `logAudit` and `auditLogList` are candidates for removal in a future pass (would need to confirm `logAudit` isn't intended for future server-side audit wiring).
- **15 `xxxList` refs read only via `getXxxByName` fallback**: in all-real-data these refs are never written (fetch-first), so the helpers always return undefined — but they remain live fallback callers in detail views. Removing them would require migrating those `detail.data.value ?? store.getXxxByName(...)` fallbacks to Vue Query only. Out of scope for this store-internal pass.

## Verify gate
- `npm run typecheck` — PASS (132 files)
- `npm run build` — PASS (built in 21.75s)
- `npm run test:unit` — PASS (487 tests, 43 files)
- `node scripts/check-await-race.mjs` — PASS
- `node scripts/check-missing-value.mjs` — PASS
